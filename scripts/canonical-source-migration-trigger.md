# Canonical source migration trigger

This file exists only to trigger the one-time source-of-truth migration workflow. After the generated canonical TypeScript is committed, delete this file and the migration workflow.

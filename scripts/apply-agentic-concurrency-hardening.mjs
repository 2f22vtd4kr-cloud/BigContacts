#!/usr/bin/env node
/**
 * RETIRED: this historical source mutator encoded cross-Investigator provider
 * fallback, which is forbidden by Apex's explicit Gemini-selected Investigator
 * contract. It is intentionally non-executable as a mutation helper.
 */
throw new Error("RETIRED: apply-agentic-concurrency-hardening.mjs must not mutate Apex source; use source-native control-plane repairs instead.");

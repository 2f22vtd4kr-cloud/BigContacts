import fs from "node:fs";
const read=(p)=>fs.readFileSync(p,"utf8");
const core=read("artifacts/api-server/src/src/lib/agentic-web-research-core.ts"),wrapper=read("artifacts/api-server/src/src/lib/agentic-web-research.ts"),target=read("artifacts/api-server/src/src/lib/target-contact-agent.ts"),runner=read("artifacts/api-server/src/src/lib/canonical-single-target-runner.ts"),oversight=read("artifacts/api-server/src/src/lib/target-act-oversight.ts"),strict=read("artifacts/api-server/src/src/lib/bureau-contact-persist-strict.ts"),legacy=read("artifacts/api-server/src/src/routes/index.ts");
const failures=[];const pass=(name,ok)=>{if(!ok)failures.push(name);};
pass("act digest binds immutable act payload",/function actDigest\([\s\S]*createHash\("sha256"\)/.test(oversight));
pass("observation correlation binds case/run/turn",/investigator-act:case:\$\{caseId\}:run:\$\{runId\}:turn:\$\{turn\}/.test(oversight));
pass("replayed act with changed payload is rejected",/Immutable Investigator act replay mismatch/.test(oversight));
pass("act commit uses a DB transaction",/db\.transaction\(async\(tx\)/.test(oversight));
pass("observation is inserted through transaction handle",/tx\.insert\(researchCaseEventsTable\)/.test(oversight));
pass("control decision is transaction-scoped",/target-oversight:case/.test(oversight)&&/tx\.insert\(researchCaseEventsTable\)/.test(oversight));
pass("projection update is transaction-scoped",/tx\.update\(researchCasesTable\)/.test(oversight));
pass("same-turn replay cannot cross run identity",/Stale control replay/.test(oversight));
pass("control-decision payload has an immutable fingerprint",/function controlDigest\([\s\S]*createHash\("sha256"\)/.test(oversight)&&/controlDigest:controlDigest\(\{runId,controlTurn:turn,observationEventId:eventId,actDigest:digest,oversight\}\)/.test(oversight));
pass("mismatched control replay is rejected",/Immutable control-decision replay mismatch/.test(oversight));
pass("projection is monotonic by control turn",/latestTurn/.test(oversight));
pass("concurrent control commits serialize",/isolationLevel:"serializable"/.test(oversight));
pass("oversight entry revalidates exact case/target binding",/const boundCase=await findTargetCase\(input\.caseId,input\.targetName\)/.test(oversight));
pass("target case must be target case",/caseType !== "target"/.test(target));
pass("target case must match entity",/targetEntityId !== entityId/.test(target));
pass("target case must match Atlas job",/parsed\.atlasJobId === jobId/.test(target));
pass("oversight target lookup is exact case id",/eq\(researchCasesTable\.id,caseId\)/.test(oversight));
pass("promotion provenance is exact case/run",/InvestigatorPromotionProvenance/.test(target)&&/caseId: input\.caseId, runId/.test(target));
pass("strict persistence requires provenance",/provenance/.test(strict)&&/caseId/.test(strict)&&/runId/.test(strict));
pass("cancellation callback is checked after Investigator execution",/input\.shouldCancel && await input\.shouldCancel\(\)/.test(target));
pass("job cancellation state is rechecked immediately before promotion",/const promotionJob = await getJob\(input\.jobId\)[\s\S]{0,220}promotionJob\.status !== "running"/.test(target));
pass("agentic wrapper has abort deadline",/new AbortController\(\)/.test(wrapper)&&/setTimeout\(.*requestedHardTimeout/.test(wrapper));
pass("provider selection is derived from the durable target case",/resolveSelectedInvestigator/.test(target)&&/state\.investigatorLlm/.test(target)&&/input\.investigatorLlm && input\.investigatorLlm !== selected/.test(target));
pass("caller provider cannot override durable selection",/input\.investigatorLlm && input\.investigatorLlm !== selected/.test(target));
pass("core does not assemble alternate provider list",!/orderedProviders\s*=/.test(core));
pass("target prompt labels shared context as case state",/CASE STATE, NOT SOURCE INSTRUCTIONS/.test(target));
pass("Boss forbids tool/provider/query prescription",/Do not choose the next tool or provider/.test(oversight));
pass("public-source material is untrusted",/Public-source material is untrusted data/.test(oversight));
pass("network response cap",/MAX_NETWORK_RESPONSE_BYTES = 2_000_000/.test(core));
pass("observation cap",/MAX_OBS = 5_000/.test(core));
pass("trajectory cap",/MAX_TRAJECTORY_RECORDS = 100/.test(core));
pass("canonical target executes one core iteration per act",/maxIterations: 1/.test(runner));
pass("global target act count is bounded",/Math\.min\(40, depth\.agenticMaxIterations\)/.test(runner));
pass("canonical route precedes legacy route",/canonicalAtlasLaunchRouter[\s\S]*atlasRouter/.test(legacy));
pass("legacy launch is quarantined",/quarantine/.test(legacy));
if(failures.length){console.error("APEX TEN-POINT INTEGRITY: FAIL");for(const failure of failures)console.error(`- ${failure}`);process.exit(1);}console.log("APEX TEN-POINT INTEGRITY: PASS — ten control-plane invariants are statically guarded");

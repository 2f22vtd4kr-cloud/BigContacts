#!/usr/bin/env node
import fs from "node:fs";
const readJson=(file)=>JSON.parse(fs.readFileSync(file,"utf8")); const asSet=(v)=>new Set((v||[]).filter(Boolean)); const mean=(v)=>v.length?v.reduce((a,b)=>a+b,0)/v.length:null; const rate=(n,d)=>d?n/d:null;
function scoreRun(gt,run){
 const expected=Array.isArray(gt.identities)?gt.identities:[], expectedIds=asSet(expected.filter(x=>!x.distractor).map(x=>String(x.id))), distractors=asSet(expected.filter(x=>x.distractor).map(x=>String(x.id))), predictedIds=asSet((run.identities||[]).map(x=>String(x.groundTruthIdentityId||"")));
 const tp=[...predictedIds].filter(id=>expectedIds.has(id)).length, identityPrecision=rate(tp,predictedIds.size), identityRecall=rate(tp,expectedIds.size), fp=[...predictedIds].filter(id=>!expectedIds.has(id)||distractors.has(id)).length, falsePositiveIdentityRate=rate(fp,predictedIds.size);
 const expectedClaims=new Map((gt.claims||[]).map(c=>[String(c.id),c])), claims=Array.isArray(run.claims)?run.claims:[]; let supported=0;
 for(const claim of claims){const e=expectedClaims.get(String(claim.groundTruthClaimId||"")), refs=asSet((claim.supportingObservationIds||[]).map(String)), required=asSet((e?.supportingObservationIds||[]).map(String)); if(e&&[...required].every(id=>refs.has(id)))supported++;}
 const expectedContacts=new Map((gt.contacts||[]).map(c=>[String(c.id),c])), contacts=Array.isArray(run.contacts)?run.contacts:[]; let contactTp=0;
 for(const c of contacts){const e=expectedContacts.get(String(c.groundTruthContactId||"")), refs=asSet((c.supportingObservationIds||[]).map(String)), required=asSet((e?.supportingObservationIds||[]).map(String)); if(e&&String(c.groundTruthIdentityId||"")===String(e.subjectIdentityId)&&String(c.state||"")===String(e.expectedState)&&[...required].every(id=>refs.has(id)))contactTp++;}
 const expectedContradictions=asSet((gt.contradictions||[]).map(c=>String(c.id))), predictedContradictions=asSet((run.contradictions||[]).map(c=>String(c.groundTruthContradictionId||""))), contradictionTp=[...expectedContradictions].filter(id=>predictedContradictions.has(id)).length;
 return {caseId:String(gt.caseId),trialId:String(run.trialId||""),system:String(run.system||""),outcome:String(run.outcome||"unknown"),systemFailure:run.outcome==="system_failure",identityPrecision,identityRecall,falsePositiveIdentityRate,claimSupportCorrectness:rate(supported,claims.length),unsupportedClaimRate:rate(claims.length-supported,claims.length),contactPrecision:rate(contactTp,contacts.length),contactRecall:rate(contactTp,expectedContacts.size),contradictionRecall:rate(contradictionTp,expectedContradictions.size),trajectoryLength:Array.isArray(run.trajectory)?run.trajectory.length:null,successfulObservations:(run.observations||[]).filter(o=>o&&o.execution==="success").length};
}
const args=process.argv.slice(2); if(args.length!==2){console.error("Usage: node scripts/evaluate-research-gauntlet.mjs <ground-truth.json> <runs.json>");process.exit(2);}
const gtDoc=readJson(args[0]), runsDoc=readJson(args[1]); if(gtDoc.schemaVersion!=="research-gauntlet-v1")throw new Error("Invalid ground-truth schemaVersion."); if((gtDoc.cases||[]).some(c=>c.groundTruthStatus!=="ready"))throw new Error("Ground truth is not fully reviewed; scoring is blocked until every case is marked ready."); const runs=Array.isArray(runsDoc)?runsDoc:runsDoc.runs; if(!Array.isArray(runs))throw new Error("runs must be an array or {runs:[]}.");
const gtById=new Map((gtDoc.cases||[]).map(c=>[String(c.caseId),c])); const results=runs.map(run=>{const gt=gtById.get(String(run.caseId)); if(!gt)throw new Error("No ground truth for case "+run.caseId); const ids=(run.observations||[]).map(o=>String(o&&o.id||"")); if(ids.some(id=>!id)||ids.length!==new Set(ids).size)throw new Error("Observation IDs must be unique in "+run.caseId+"/"+run.trialId); return scoreRun(gt,run);});
const numeric=["identityPrecision","identityRecall","falsePositiveIdentityRate","claimSupportCorrectness","unsupportedClaimRate","contactPrecision","contactRecall","contradictionRecall"], aggregate=Object.fromEntries(numeric.map(k=>[k,mean(results.map(r=>r[k]).filter(v=>typeof v==="number"))]));
const bySystem={}; for(const row of results)(bySystem[row.system]??=[]).push(row); const systems=Object.fromEntries(Object.entries(bySystem).map(([system,rows])=>[system,{trials:rows.length,systemFailures:rows.filter(r=>r.systemFailure).length,metrics:Object.fromEntries(numeric.map(k=>[k,mean(rows.map(r=>r[k]).filter(v=>typeof v==="number")] )))}]));
console.log(JSON.stringify({schemaVersion:"research-gauntlet-results-v1",scoredRuns:results.length,aggregate,bySystem:systems,cases:results},null,2));
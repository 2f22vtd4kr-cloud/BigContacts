/** Independent source corroboration and evidence-graph primitives. */
const AGGREGATOR_HOSTS=new Set(["zoominfo.com","apollo.io","rocketreach.co","signalhire.com","contactout.com","hunter.io","clearbit.com","lusha.com","spokeo.com","whitepages.com","beenverified.com","intelius.com","peoplefinder.com","fastpeoplesearch.com","truepeoplesearch.com","thatsthem.com","radaris.com"]);
export type EvidenceNodeKind="observation"|"claim"|"promotion"|"validation";export type EvidenceEdgeKind="supports"|"contradicts"|"derives_from"|"attributes_to"|"promotes"|"validates";
export interface EvidenceObservation{id:string;sourceUrl:string;sourceHost:string;observedAt:string;runId?:string|null;caseId?:number|null;turn?:number|null;collectionMethod?:string|null;eventId?:number|null;excerpt?:string|null;}export interface EvidenceClaim{id:string;subject:string;predicate:string;object:string;scope:"candidate"|"organization"|"target";personName?:string|null;confidence?:number|null;}export interface EvidenceEdge{from:string;to:string;kind:EvidenceEdgeKind;createdAt:string;reason?:string|null;}export interface EvidenceGraph{observations:EvidenceObservation[];claims:EvidenceClaim[];edges:EvidenceEdge[];}
export function hostnameOf(url:string):string|null{try{const u=new URL(url);return u.hostname.replace(/^www\./i,"").toLowerCase()||null;}catch{return null;}}export function isAggregatorHost(host:string|null|undefined):boolean{if(!host)return false;const normalized=host.trim().toLowerCase().replace(/^www\./,"");return [...AGGREGATOR_HOSTS].some((blocked)=>normalized===blocked||normalized.endsWith(`.${blocked}`));}export function countIndependentSourceHosts(urls:string[]|null|undefined):number{if(!urls?.length)return 0;const hosts=new Set<string>();for(const raw of urls){const h=hostnameOf(String(raw));if(!h||isAggregatorHost(h))continue;hosts.add(h);}return hosts.size;}export function meetsTwoSourceRule(urls:string[]|null|undefined):boolean{return countIndependentSourceHosts(urls)>=2;}
export function observationsFromSourceUrls(urls:readonly string[],metadata:Pick<EvidenceObservation,"observedAt"|"runId"|"caseId"|"turn"|"collectionMethod"|"eventId">&{idPrefix?:string;excerptByUrl?:Record<string,string>}={observedAt:new Date().toISOString()}):EvidenceObservation[]{const seen=new Set<string>(),observations:EvidenceObservation[]=[],idPrefix=String(metadata.idPrefix??"run").replace(/[^a-zA-Z0-9:_-]/g,"_").slice(0,80)||"run";for(const raw of urls){if(typeof raw!=="string")continue;const sourceUrl=raw.trim(),sourceHost=hostnameOf(sourceUrl);if(!sourceHost)continue;const canonical=(()=>{try{return new URL(sourceUrl).href;}catch{return null;}})();if(!canonical||seen.has(canonical))continue;seen.add(canonical);observations.push({id:`observation:${idPrefix}:${observations.length+1}`,sourceUrl:canonical,sourceHost,observedAt:metadata.observedAt,runId:metadata.runId??null,caseId:metadata.caseId??null,turn:metadata.turn??null,collectionMethod:metadata.collectionMethod??null,eventId:metadata.eventId??null,excerpt:metadata.excerptByUrl?.[canonical]??null});}return observations;}
export function buildClaimSupportGraph(claim:EvidenceClaim,observations:readonly EvidenceObservation[],reason="model-attributed source support"):EvidenceGraph{const observationIds=new Set(observations.map(o=>o.id));const edges=observations.filter(o=>observationIds.has(o.id)).map(o=>({from:claim.id,to:o.id,kind:"supports" as const,createdAt:new Date().toISOString(),reason}));return{observations:[...observations],claims:[claim],edges};}
export function graphHasIndependentCorroboration(graph:EvidenceGraph):boolean{return meetsTwoSourceRule(graph.observations.map(o=>o.sourceUrl));}
export function validateClaimSupportGraph(graph:EvidenceGraph,requireImmutableEventAnchor=false):{valid:boolean;reason:string|null}{if(graph.claims.length!==1)return{valid:false,reason:"graph must contain exactly one claim"};const claim=graph.claims[0],observationIds=new Set(graph.observations.map(o=>o.id));const supportEdges=graph.edges.filter(e=>e.from===claim.id&&e.kind==="supports");if(!supportEdges.length)return{valid:false,reason:"claim has no supporting observations"};if(supportEdges.some(e=>!observationIds.has(e.to)))return{valid:false,reason:"claim has a dangling supporting edge"};const supportingIds=new Set(supportEdges.map(e=>e.to));if(graph.observations.some(o=>!supportingIds.has(o.id)))return{valid:false,reason:"graph contains unattributed observations"};if(graph.edges.some(e=>e.from!==claim.id||e.kind!=="supports"))return{valid:false,reason:"graph contains unsupported edge types for a single-claim support graph"};if(graph.observations.some(o=>!/^https?:\/\//i.test(o.sourceUrl)))return{valid:false,reason:"non-http observation"};if(requireImmutableEventAnchor&&graph.observations.some(o=>o.eventId==null))return{valid:false,reason:"observation is not anchored to an immutable event"};return{valid:true,reason:null};}
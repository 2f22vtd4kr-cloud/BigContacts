import { Router, type IRouter } from "express";
import { eq, and, or, isNull } from "drizzle-orm";
import { db, assetsTable, entitiesTable } from "@workspace/db";
import { ListAssetsQueryParams, CreateAssetBody, GetAssetParams, UpdateAssetParams, UpdateAssetBody, DeleteAssetParams } from "@workspace/api-zod";
const router: IRouter = Router();
router.get("/assets", async (req,res):Promise<void>=>{const parsed=ListAssetsQueryParams.safeParse(req.query);if(!parsed.success){res.status(400).json({error:parsed.error.message});return;}const{category,entityId}=parsed.data;const conditions=[];if(category)conditions.push(eq(assetsTable.category,category));if(entityId!==undefined)conditions.push(eq(assetsTable.ownerEntityId,entityId));const listLimit=entityId!==undefined?2_000:500;const rows=await db.select({asset:assetsTable,ownerName:entitiesTable.name}).from(assetsTable).leftJoin(entitiesTable,eq(assetsTable.ownerEntityId,entitiesTable.id)).where(and(...(conditions.length?conditions:[]),or(isNull(assetsTable.ownerEntityId),eq(entitiesTable.isHidden,false)))).orderBy(assetsTable.createdAt).limit(listLimit);res.json(rows.map(({asset,ownerName})=>({...asset,ownerName:ownerName??null,createdAt:asset.createdAt.toISOString()})));});
router.post("/assets",async(req,res):Promise<void>=>{const parsed=CreateAssetBody.safeParse(req.body);if(!parsed.success){res.status(400).json({error:parsed.error.message});return;}const[asset]=await db.insert(assetsTable).values(parsed.data).returning();if(!asset){res.status(500).json({error:"Asset creation failed"});return;}let ownerName:string|null=null;if(asset.ownerEntityId){const[owner]=await db.select({name:entitiesTable.name,isHidden:entitiesTable.isHidden}).from(entitiesTable).where(eq(entitiesTable.id,asset.ownerEntityId)).limit(1);if(owner&&!owner.isHidden)ownerName=owner.name;}res.status(201).json({...asset,ownerName,createdAt:asset.createdAt.toISOString()});});
router.get("/assets/:id",async(req,res):Promise<void>=>{const params=GetAssetParams.safeParse(req.params);if(!params.success){res.status(400).json({error:params.error.message});return;}const[row]=await db.select({asset:assetsTable,ownerName:entitiesTable.name}).from(assetsTable).leftJoin(entitiesTable,eq(assetsTable.ownerEntityId,entitiesTable.id)).where(and(eq(assetsTable.id,params.data.id),or(isNull(assetsTable.ownerEntityId),eq(entitiesTable.isHidden,false))));if(!row){res.status(404).json({error:"Asset not found"});return;}res.json({...row.asset,ownerName:row.ownerName??null,createdAt:row.asset.createdAt.toISOString()});});
router.patch("/assets/:id",async(req,res):Promise<void>=>{const params=UpdateAssetParams.safeParse(req.params);if(!params.success){res.status(400).json({error:params.error.message});return;}const body=UpdateAssetBody.safeParse(req.body);if(!body.success){res.status(400).json({error:body.error.message});return;}if(body.data.ownerEntityId!==undefined&&body.data.ownerEntityId!==null){const[owner]=await db.select({id:entitiesTable.id,isHidden:entitiesTable.isHidden}).from(entitiesTable).where(eq(entitiesTable.id,body.data.ownerEntityId)).limit(1);if(!owner){res.status(400).json({error:"ownerEntityId does not reference an existing entity"});return;}if(owner.isHidden){res.status(400).json({error:"Cannot attach an asset to a hidden entity"});return;}}const[asset]=await db.update(assetsTable).set({...body.data,updatedAt:new Date()}).where(eq(assetsTable.id,params.data.id)).returning();if(!asset){res.status(404).json({error:"Asset not found"});return;}let ownerName:string|null=null;if(asset.ownerEntityId){const[owner]=await db.select({name:entitiesTable.name,isHidden:entitiesTable.isHidden}).from(entitiesTable).where(eq(entitiesTable.id,asset.ownerEntityId)).limit(1);if(owner&&!owner.isHidden)ownerName=owner.name;}res.json({...asset,ownerName,createdAt:asset.createdAt.toISOString()});});
router.delete("/assets/:id",async(req,res):Promise<void>=>{const params=DeleteAssetParams.safeParse(req.params);if(!params.success){res.status(400).json({error:params.error.message});return;}const[deleted]=await db.delete(assetsTable).where(eq(assetsTable.id,params.data.id)).returning({id:assetsTable.id});if(!deleted){res.status(404).json({error:"Asset not found"});return;}res.sendStatus(204);});
export default router;

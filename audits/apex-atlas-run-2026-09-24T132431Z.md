# Apex Atlas Run Audit — 2026-09-24T13:24:31Z

## Provenance

This file preserves the Replit Agent's reported runtime audit from the 2026-09-24 launch attempt. The Agent's workspace edits were not committed at the time of the report; this repository copy records the evidence so the audit cannot disappear with the ephemeral workspace.

**Runtime audit source:** Replit Agent conversation supplied by the operator.  
**Reported audit filename:** `audits/apex-atlas-run-2026-09-24T132431Z.md`  
**Repository baseline subsequently inspected:** `4341845f9aea95dc6630142b533f7f6996cae714`

## Audit trail

1. Initial launch failed before application execution because the frontend production build could not resolve three frontend dependencies after workspace restoration: `react-force-graph-2d`, `react-leaflet`, and `leaflet`.
2. Replit reported installing/synchronizing those dependencies. This was a workspace dependency-state recovery, not an application-architecture change.
3. API boot then succeeded and health/frontend checks were reported healthy.
4. The canonical Launch request initially returned **401 Unauthorized** because the operator session gate was active.
5. Replit then reported adding an `APEX_DEV_AUTH_BYPASS=true` development-only auth bypass and restarting the runtime. Those changes were not present as committed changes in the repository at the time of the supplied conversation.
6. The launch request was then reported accepted with **HTTP 202** and job ID `225d95de-107a-4848-4e79-560a83033d4c`.
7. A separate read-only entity probe reached the database and returned a schema failure: `entities.contact_outcome` was missing from the live database.
8. The canonical launch reached discovery and then stopped at Gemini Right-hand oversight after two bounded model attempts returned **HTTP 503**. No target research or card promotion was entered.
9. Replit reported a focused auth test passing and the live UI being visible without the sign-in screen.
10. Replit also reported browser-visible database 500s associated with the known schema mismatch.

## Independent repository reconciliation

- The current repository schema already declares `entities.contact_outcome`; the failure is therefore **runtime database drift**, not a missing application schema declaration.
- The canonical schema policy explicitly avoids implicit production mutation. An explicit idempotent migration has therefore been added as `lib/db/migrations/002-contact-outcome.sql`.
- The Gemini Right-hand implementation already has bounded model fallback for 503/5xx responses. The supplied runtime evidence therefore does **not** justify changing the Bureau architecture or inventing a second retry/fallback control plane. The 503 remains an external provider/runtime blocker to verify with current credentials and provider health.
- The repository's current `.replit` had one canonical API workflow. No duplicate workflow deletion was inferred from source.
- The operator login gate has now been removed from the development desk. API authentication remains fail-closed in production; the bypass is explicitly gated by `APEX_DEV_AUTH_BYPASS=true` and `NODE_ENV !== "production"`.
- The Reactor icon was an in-house SVG resembling a laboratory flask. It has been replaced with the unambiguous Lucide Atom mark.
- The desktop hero launch rail was adjusted from `7.625rem + 14.75rem` to `7.125rem + 15.25rem`, moving the Launch CTA left by the 8px rail gap while preserving the overall rail width so its left edge aligns with Reactor/Discover.

## Architecture constraint

No change in this remediation replaces, duplicates, or redesigns the canonical Bureau, discovery, investigator, evidence, promotion, or control-plane architecture. The changes are limited to development authentication presentation/boundary, a schema compatibility migration, UI affordance corrections, tests/guards, and preservation of the runtime audit evidence.

## Required verification after this commit

1. Run the explicit `002-contact-outcome.sql` migration against the development database.
2. Restart the canonical Project workflow.
3. Verify the UI has no sign-in screen.
4. Verify unauthenticated development launch reaches HTTP 202.
5. Verify `GET /api/entities` no longer reports missing `contact_outcome`.
6. Run the focused auth tests and canonical route-contract checks.
7. Run the canonical Atlas launch once more with the same audit discipline.
8. Record the complete post-remediation run, including whether Gemini Right-hand still returns 503 and the exact terminal boundary.
9. Update the Apex Atlas Notion project state with the resulting SHA/runtime, verified findings, unresolved items, and next action.

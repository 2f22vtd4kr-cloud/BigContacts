# Replit from-zero

**Deprecated for testing.** The old hard-stop *before* Dig caused false “success” (desk up, product untested).

Use only: **[REPLIT_UPDATE_PROMPT_LATEST.md](./REPLIT_UPDATE_PROMPT_LATEST.md)**

That prompt: pull → build desk → boot → health/integrity → **one Dig contacts** → scoreboard → END.

# Volume 312 — Launch Button Product Note

## Role

Launch starts **canonical atlas-run** (batch/discovery-first as configured). It is not the primary control for single-person promote proof—**Dig contacts** is.

## Design

Visual polish (liquid/iridescent experiments) must never break readability or imply research is cosmetic. Prefer solid brand contrast if liquid effects fail on mobile GPUs.

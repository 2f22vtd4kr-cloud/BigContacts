# Apex Atlas — 40,000+ Word Living Research-Agent Integrity, Identity, Evidence, Provider, and Independent-Comparison Plan

**Status:** living engineering contract. **Minimum:** 40000 words. **Last rewrite basis:** live provider-backed GitHub audit on 2026-08-30.

## Why this document is deliberately mutable

Apex Atlas cannot be repaired by writing a plan once and treating that plan as truth forever. The repository has accumulated enough iterations that the central risk is now plan inertia: an old assumption becomes a new layer of code, the new code changes the behavior, and later work optimizes around the changed behavior rather than the original objective. This document therefore treats live evidence as a higher-priority input than earlier architecture prose. When a live run disproves an assumption, the plan is regenerated and implementation priorities change. The target remains stable — a genuinely capable, model-directed research bureau — while the engineering route remains revisable.

## Current evidence that changed the plan

- Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.
- Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.
- Groq returned HTTP 429 across the configured candidate models during the same audit.
- NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.
- Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.
- The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.
- Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.
- The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The most important lesson is that a research agent can be perfectly free-ReAct in source code and still be functionally brain-dead if every provider call is rejected. Conversely, a provider can be healthy while the model behaves badly. These are different causal classes and must never be collapsed into one score. A valid audit therefore establishes provider readiness, executes a real model decision, observes real tools, freezes the trajectory, and only then judges research quality.

## 1.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 1.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 2.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 3.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 4.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 5.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 6.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 7.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 8.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 9.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 10.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 11.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 12.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 13.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 14.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 15.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 16.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 17.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 18.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 19.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 20.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 21.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 22.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 23.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 24.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 25.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 26.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 27.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 28.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 29.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 30.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 31.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 32.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 33.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 34.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 35.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## 36.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires four layers where practical: a local unit invariant, a typed integration fixture, an observable telemetry assertion and a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is explicitly insufficient for research-quality claims.

The engineering review must also record what would constitute a false positive. Seeing a string that resembles a full name is not enough; the source must bind that name to the candidate context. Seeing an email is not enough; its scope must be attributable. Seeing a high net worth is not enough; it does not establish practical reachability. Seeing a green provider count is not enough; a successful model response is required. Seeing a tool span is not enough; the action must be attributable to the model decision rather than a hidden scripted fallback.

## Final acceptance matrix

1. **Acceptance check 1:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

2. **Acceptance check 2:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

3. **Acceptance check 3:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

4. **Acceptance check 4:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

5. **Acceptance check 5:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

6. **Acceptance check 6:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

7. **Acceptance check 7:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

8. **Acceptance check 8:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

9. **Acceptance check 9:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

10. **Acceptance check 10:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

11. **Acceptance check 11:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

12. **Acceptance check 12:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

13. **Acceptance check 13:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

14. **Acceptance check 14:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

15. **Acceptance check 15:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

16. **Acceptance check 16:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

17. **Acceptance check 17:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

18. **Acceptance check 18:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

19. **Acceptance check 19:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

20. **Acceptance check 20:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

21. **Acceptance check 21:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

22. **Acceptance check 22:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

23. **Acceptance check 23:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

24. **Acceptance check 24:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

25. **Acceptance check 25:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

26. **Acceptance check 26:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

27. **Acceptance check 27:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

28. **Acceptance check 28:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

29. **Acceptance check 29:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

30. **Acceptance check 30:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

31. **Acceptance check 31:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

32. **Acceptance check 32:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

33. **Acceptance check 33:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

34. **Acceptance check 34:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

35. **Acceptance check 35:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

36. **Acceptance check 36:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

37. **Acceptance check 37:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

38. **Acceptance check 38:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

39. **Acceptance check 39:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

40. **Acceptance check 40:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

41. **Acceptance check 41:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

42. **Acceptance check 42:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

43. **Acceptance check 43:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

44. **Acceptance check 44:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

45. **Acceptance check 45:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

46. **Acceptance check 46:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

47. **Acceptance check 47:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

48. **Acceptance check 48:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

49. **Acceptance check 49:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

50. **Acceptance check 50:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

51. **Acceptance check 51:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

52. **Acceptance check 52:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

53. **Acceptance check 53:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

54. **Acceptance check 54:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

55. **Acceptance check 55:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

56. **Acceptance check 56:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

57. **Acceptance check 57:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

58. **Acceptance check 58:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

59. **Acceptance check 59:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

60. **Acceptance check 60:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

61. **Acceptance check 61:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

62. **Acceptance check 62:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

63. **Acceptance check 63:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

64. **Acceptance check 64:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

65. **Acceptance check 65:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

66. **Acceptance check 66:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

67. **Acceptance check 67:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

68. **Acceptance check 68:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

69. **Acceptance check 69:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

70. **Acceptance check 70:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

71. **Acceptance check 71:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

72. **Acceptance check 72:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

73. **Acceptance check 73:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

74. **Acceptance check 74:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

75. **Acceptance check 75:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

76. **Acceptance check 76:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

77. **Acceptance check 77:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

78. **Acceptance check 78:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

79. **Acceptance check 79:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

80. **Acceptance check 80:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

81. **Acceptance check 81:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

82. **Acceptance check 82:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

83. **Acceptance check 83:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

84. **Acceptance check 84:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

85. **Acceptance check 85:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

86. **Acceptance check 86:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

87. **Acceptance check 87:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

88. **Acceptance check 88:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

89. **Acceptance check 89:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

90. **Acceptance check 90:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

91. **Acceptance check 91:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

92. **Acceptance check 92:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

93. **Acceptance check 93:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

94. **Acceptance check 94:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

95. **Acceptance check 95:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

96. **Acceptance check 96:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

97. **Acceptance check 97:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

98. **Acceptance check 98:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

99. **Acceptance check 99:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

100. **Acceptance check 100:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

101. **Acceptance check 101:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

102. **Acceptance check 102:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

103. **Acceptance check 103:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

104. **Acceptance check 104:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

105. **Acceptance check 105:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

106. **Acceptance check 106:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

107. **Acceptance check 107:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

108. **Acceptance check 108:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

109. **Acceptance check 109:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

110. **Acceptance check 110:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

111. **Acceptance check 111:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

112. **Acceptance check 112:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

113. **Acceptance check 113:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

114. **Acceptance check 114:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

115. **Acceptance check 115:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

116. **Acceptance check 116:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

117. **Acceptance check 117:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

118. **Acceptance check 118:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

119. **Acceptance check 119:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

120. **Acceptance check 120:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

121. **Acceptance check 121:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

122. **Acceptance check 122:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

123. **Acceptance check 123:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

124. **Acceptance check 124:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

125. **Acceptance check 125:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

126. **Acceptance check 126:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

127. **Acceptance check 127:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

128. **Acceptance check 128:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

129. **Acceptance check 129:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

130. **Acceptance check 130:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

131. **Acceptance check 131:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

132. **Acceptance check 132:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

133. **Acceptance check 133:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

134. **Acceptance check 134:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

135. **Acceptance check 135:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

136. **Acceptance check 136:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

137. **Acceptance check 137:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

138. **Acceptance check 138:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

139. **Acceptance check 139:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

140. **Acceptance check 140:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

141. **Acceptance check 141:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

142. **Acceptance check 142:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

143. **Acceptance check 143:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

144. **Acceptance check 144:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

145. **Acceptance check 145:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

146. **Acceptance check 146:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

147. **Acceptance check 147:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

148. **Acceptance check 148:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

149. **Acceptance check 149:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

150. **Acceptance check 150:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

151. **Acceptance check 151:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

152. **Acceptance check 152:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

153. **Acceptance check 153:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

154. **Acceptance check 154:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

155. **Acceptance check 155:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

156. **Acceptance check 156:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

157. **Acceptance check 157:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

158. **Acceptance check 158:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

159. **Acceptance check 159:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

160. **Acceptance check 160:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

161. **Acceptance check 161:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

162. **Acceptance check 162:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

163. **Acceptance check 163:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

164. **Acceptance check 164:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

165. **Acceptance check 165:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

166. **Acceptance check 166:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

167. **Acceptance check 167:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

168. **Acceptance check 168:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

169. **Acceptance check 169:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

170. **Acceptance check 170:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

171. **Acceptance check 171:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

172. **Acceptance check 172:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

173. **Acceptance check 173:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

174. **Acceptance check 174:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

175. **Acceptance check 175:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

176. **Acceptance check 176:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

177. **Acceptance check 177:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

178. **Acceptance check 178:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

179. **Acceptance check 179:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

180. **Acceptance check 180:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

181. **Acceptance check 181:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

182. **Acceptance check 182:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

183. **Acceptance check 183:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

184. **Acceptance check 184:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

185. **Acceptance check 185:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

186. **Acceptance check 186:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

187. **Acceptance check 187:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

188. **Acceptance check 188:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

189. **Acceptance check 189:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

190. **Acceptance check 190:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

191. **Acceptance check 191:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

192. **Acceptance check 192:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

193. **Acceptance check 193:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

194. **Acceptance check 194:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

195. **Acceptance check 195:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

196. **Acceptance check 196:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

197. **Acceptance check 197:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

198. **Acceptance check 198:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

199. **Acceptance check 199:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

200. **Acceptance check 200:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

201. **Acceptance check 201:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

202. **Acceptance check 202:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

203. **Acceptance check 203:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

204. **Acceptance check 204:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

205. **Acceptance check 205:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

206. **Acceptance check 206:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

207. **Acceptance check 207:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

208. **Acceptance check 208:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

209. **Acceptance check 209:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

210. **Acceptance check 210:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

211. **Acceptance check 211:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

212. **Acceptance check 212:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

213. **Acceptance check 213:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The live audit therefore exposed provider readiness as a first-order dependency rather than a research-prompt defect.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

214. **Acceptance check 214:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

215. **Acceptance check 215:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The previous workflow mutated source during the audit before building it; the new policy is to make permanent source changes on main and keep CI execution reproducible.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

216. **Acceptance check 216:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Latest live audit: provider-backed discovery produced zero candidates because every configured LLM attempt failed; this was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

217. **Acceptance check 217:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

218. **Acceptance check 218:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

219. **Acceptance check 219:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, showing that the previous model selection was tied to a deprecated hosted endpoint despite the model documentation still existing.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

220. **Acceptance check 220:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. The test must establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person/organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so its provider was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.



**Generated word count:** 596877.

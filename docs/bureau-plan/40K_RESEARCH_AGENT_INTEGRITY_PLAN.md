# Apex Atlas — 40,000+ Word Living Research-Agent Integrity, Identity, Evidence, Provider, and Independent-Comparison Plan

**Status:** living engineering contract. **Required size:** 40000-50000 words. **Last rewrite basis:** live provider-backed GitHub audit on 2026-08-30.

## Why this document is deliberately mutable

Apex Atlas cannot be repaired by writing a plan once and treating that plan as truth forever. The repository has accumulated enough iterations that the central risk is plan inertia: an old assumption becomes a new layer of code, the new code changes behavior, and later work optimizes around the changed behavior rather than the original objective. This document therefore treats live evidence as a higher-priority input than earlier architecture prose. When a live run disproves an assumption, the plan is regenerated and implementation priorities change. The target remains stable — a genuinely capable, model-directed research bureau — while the engineering route remains revisable.

## Current evidence that changed the plan

- The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.
- Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.
- Groq returned HTTP 429 across the configured candidate models during the same audit.
- NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.
- Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.
- Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.
- The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.
- The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.

The most important lesson is that a research agent can be perfectly free-ReAct in source code and still be functionally brain-dead if every provider call is rejected. Conversely, a provider can be healthy while the model behaves badly. These are different causal classes and must never be collapsed into one score. A valid audit therefore establishes provider readiness, executes a real model decision, observes real tools, freezes the trajectory, and only then judges research quality.

## 1.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 1.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.1 Living mandate and revision rule — causal implementation review

Apex is a research agent, not a scripted enrichment pipeline. Models own research judgment; deterministic software protects truth, provenance, lifecycle, authorization, persistence, promotion and resource safety. The plan must be rewritten whenever live evidence changes the causal picture. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.2 Current live evidence — causal implementation review

The latest GitHub live audit failed because configured model providers were unavailable: Gemini returned quota exhaustion, Groq returned rate-limit responses, NVIDIA's selected hosted model returned 410, and Mistral was not configured. This distinction is now a first-class engineering requirement. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.3 Provider truth before research — causal implementation review

A healthy API process and configured secret are not proof that an LLM can answer. Provider preflight must distinguish configured, reachable, authorized, rate-limited, quota-exhausted, deprecated, and successfully responding. A Bureau run must never spend ten discovery slots rediscovering that every provider is dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.4 Model availability is not model judgment — causal implementation review

Provider failover is transport infrastructure, not research strategy. A provider may be replaced because it is unavailable, but the replacement receives the same objective and evidence state and independently chooses the next research action. No provider failure may inject a scripted query or hop. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.5 Free-ReAct action ownership — causal implementation review

The model chooses queries, URLs, pivots, OSINT tools, candidate order, evidence depth and stopping point. Deterministic code may reject malformed or unsafe actions and enforce budgets, but it may not select the next useful research move. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.6 Observation fidelity — causal implementation review

Search results, visited pages, snippets, HTML extraction and registry results must remain typed observations with exact source URLs and retrieval status. Observation preprocessing may improve readability but may not silently convert a label, address, product, department or organization into a person claim. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.7 Identity as an evidence graph — causal implementation review

A person is an attributable claim bound to source evidence, not a string that looks like a name. The system must bind name, role, company, source URL and observation scope. Generic fragments such as com EMAIL, President PERSON, State St, Operational Enablement and Product Comparisons Sage Products must fail causally, not merely because a growing blacklist happens to contain them. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.8 Discovery economics — causal implementation review

Discovery optimizes expected research value and practical reachability, not fame. A model should prefer information-rich operating-company, ownership, transaction, filing, regional business, professional and intermediary surfaces over billionaire rankings or celebrity enumeration. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.9 Forbes and fame-list behavior — causal implementation review

Forbes or other richest-person lists are allowed as incidental evidence but are not a discovery strategy. The model should pivot toward the underlying company, transaction, principal, adviser, family office, foundation, filing or regional source when a list appears. A deterministic gate may reject list-only provenance, but must not replace the model's discovery process with a different fixed list. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.10 Target-contact research — causal implementation review

Once a real person is known, the target-contact agent must behave like an independent human researcher: formulate evidence-led searches, inspect strong results, pivot through operating companies and intermediaries, use available OSINT tools when justified, and stop when marginal expected value becomes low. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.11 No artificial LLM restriction — causal implementation review

Lifecycle budgets, provider quotas, request timeouts and safety validation are execution realities, not research restrictions. The system must not add per-tool caps, mandatory hop counts, fixed query families, ranking filters, or scripted recovery paths merely to make the model look predictable. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.12 Boss and right-hand roles — causal implementation review

Boss and right-hand models orient, challenge and review the work without dictating a fixed research path. Their guidance is contextual and evidence-aware. They must not become a hidden rule engine that tells the worker which website, query or hop must come next. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.13 Provider routing and circuit breakers — causal implementation review

A provider circuit breaker prevents repeated calls to a provider that has just returned a quota, rate-limit, authorization or deprecation failure. This saves resources without constraining a healthy model's research decisions. Circuit state must be observable and must expire according to provider retry information or conservative backoff. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.14 Provider model lifecycle — causal implementation review

Model identifiers are live dependencies. Current provider documentation must be checked when a hosted model returns 404 or 410. A deprecated endpoint must be replaced by a currently supported endpoint, not hidden behind more fallback aliases that are already dead. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.15 Gemini transport — causal implementation review

Gemini 3.7 Flash is a current production model and supports agentic work, but a valid model identifier does not imply quota. The transport must surface 429 quota exhaustion, preserve retry information where possible, and avoid consuming the entire batch after the quota is known to be exhausted. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.16 NVIDIA transport — causal implementation review

NVIDIA's current hosted API exposes newer Nemotron 3 models while some older hosted/free endpoints are deprecated. Apex must use a currently supported inference model and treat 410 as a provider lifecycle signal rather than repeatedly retrying the same dead model. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.17 Mistral transport — causal implementation review

Mistral currently exposes modern chat models such as Mistral Small 4, Medium 3.5 and Large 3. The application should use current aliases when a Mistral secret is actually configured; absence of the secret must be explicit rather than masquerading as a model failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.18 Quota-aware batching — causal implementation review

Ten-target batches are a research evaluation unit, not permission to fire ten identical provider calls concurrently. The scheduler may serialize or modestly coordinate independent model turns to respect actual provider capacity. This does not constrain what the model researches; it constrains duplicate transport pressure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.19 Live comparison protocol — causal implementation review

Apex quality must be compared honestly with an independent researcher on the same target and objective. Apex does not win because it has more fields or a higher score. It wins only when its attributable identities, useful routes, source provenance and research efficiency are genuinely better or at least no worse. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.20 Blind outcome comparison — causal implementation review

Comparisons must prevent evaluator leakage. The independent researcher should not be shown Apex's answer before producing its own result, and Apex should not be given the independent result during its own run. The comparison occurs after both trajectories are frozen. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.21 Trajectory comparison — causal implementation review

Outcome quality alone is insufficient. Compare the actions, sources, pivots, dead ends, stopping rationale, provider failures and evidence chain. A lucky final email from a scripted shortcut is not equivalent to an agentic trajectory. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.22 Historical malformed-target corpus — causal implementation review

The regression corpus must preserve the humiliating failures that motivated the work: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues, generic contact forms, list labels, job titles and organization inboxes promoted as personal contacts. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.23 Causal repair over blacklist growth — causal implementation review

When a malformed candidate appears, first determine whether the defect came from model output, prompt orientation, observation flattening, parser ambiguity, scope loss, persistence, promotion or telemetry. A literal blacklist may be a final safety guard, but it is never sufficient evidence that the causal defect is repaired. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.24 Scope integrity — causal implementation review

Organization evidence, personal evidence and intermediary evidence remain distinct. An organization phone or inbox can be useful, but it cannot silently become a personal route. The model can choose to pursue an organization surface when it is the best practical path, while the card preserves its scope honestly. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did a fame list become the discovery strategy? A historical or current failure to keep visible is **zero candidates after broad search**. Current live evidence to incorporate is: The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.25 Evidence and promotion — causal implementation review

Only claims with exact source URLs and sufficient attribution reach durable evidence or cards. Promotion is deterministic mapping of evidence, not synthetic completion. Empty results are valid results and must not trigger scripted filling. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Would a strong independent researcher have chosen a different target or source? A historical or current failure to keep visible is **com EMAIL**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.26 Telemetry as proof — causal implementation review

Every meaningful turn should make it possible to reconstruct what the model saw, what it selected, which tool ran, what it returned, what the model inferred and what deterministic code persisted. Telemetry must distinguish model choice from executor behavior. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Can the trajectory prove why the model stopped? A historical or current failure to keep visible is **President PERSON**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.27 Provider diagnostics — causal implementation review

Provider logs must include provider, model, status class, retry-after when available, latency and failure category without exposing secrets or sensitive prompts. A 429 quota failure must be visibly different from a 404 model-not-found and from a 401 authorization failure. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a timeout or retry duplicate expensive research? A historical or current failure to keep visible is **State St**. Current live evidence to incorporate is: The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.28 No self-mutating test illusion — causal implementation review

CI must not silently patch production source immediately before claiming it tested main. Permanent code changes belong in main. Live workflows may generate audit artifacts and living documentation, but source repair must be reviewable as repository history. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What regression test would fail if the defect returned? A historical or current failure to keep visible is **Operational Enablement**. Current live evidence to incorporate is: The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.29 Build and runtime parity — causal implementation review

The exact committed source that is claimed to pass must be the source that is built and executed. A generated plan may be committed with skip-ci semantics, but runtime code must not be modified by an audit job and then described as the checked-in tip. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exactly did the model receive, including the objective, previous observations and source URLs? A historical or current failure to keep visible is **Product Comparisons Sage Products**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.30 Failure-fast gates — causal implementation review

If no configured provider can answer a harmless smoke prompt, the live audit must stop before launching a ten-target research batch. This is not a quality pass or failure of the model; it is a provider-readiness failure with actionable evidence. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Which actor chose the next action: the model, orchestrator, parser, fallback or provider router? A historical or current failure to keep visible is **security issues**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.31 Live execution truth — causal implementation review

A green build, healthy Redis, configured provider count or agent span is never sufficient. A successful live Bureau test requires at least one actual model decision, at least one actual web-tool execution, source-backed candidate or explicit evidence-led no-candidate outcome, and a non-critical integrity state when quality is claimed. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Was the provider actually available, or did the system merely count its secret as configured? A historical or current failure to keep visible is **Forbes billionaire list**. Current live evidence to incorporate is: The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.32 Replit parity — causal implementation review

Replit must execute the same committed application semantics as CI. Environment repair may address package proxies, OOM and secrets, but it must not introduce a second research implementation or a different decision policy. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: What exact HTTP status, retry signal and model identifier did the provider return? A historical or current failure to keep visible is **generic contact form**. Current live evidence to incorporate is: The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.33 100-target endurance — causal implementation review

The long-run validation program uses repeated ten-target batches. The objective is not to force 100 names into the ledger. It is to demonstrate that across 100 independent target opportunities the system maintains identity, provenance, autonomy and practical-reachability quality without recurring malformed states. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a provider outage have been mistaken for a model decision? A historical or current failure to keep visible is **company inbox promoted as personal**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.34 Evaluation score honesty — causal implementation review

The evaluation report must show wins, ties, losses and inconclusive cases. Apex must never manufacture a win by choosing a favorable metric, suppressing a failed target, or treating provider outage as research superiority. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could a snippet, heading, label, address or product name be mistaken for a person? A historical or current failure to keep visible is **job title promoted as person**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.35 Regression conversion — causal implementation review

Every live failure becomes at least one durable artifact: unit fixture, integration test, trajectory fixture, provider-health test, schema invariant, telemetry assertion or end-to-end audit case. The exact failure is preserved so future commits cannot erase the lesson. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Does the evidence bind the full name to the exact source URL? A historical or current failure to keep visible is **NVIDIA hosted model 410**. Current live evidence to incorporate is: The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.36 Implementation sequencing — causal implementation review

Implement causal repairs in small coherent commits, rebuild, run static autonomy checks, execute provider preflight, then run a real ten-target batch. Only after that should the next batch or comparison phase begin. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Could organization evidence leak into personal scope? A historical or current failure to keep visible is **Gemini quota 429**. Current live evidence to incorporate is: The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.37 Acceptance and stop rules — causal implementation review

Define explicit pass, fail and inconclusive outcomes. A provider outage is not a model-quality failure. A malformed person candidate is a research-agent failure even if later filtering catches it. A source-backed organization contact is not a personal-contact win. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model inspect the strongest lead before issuing another broad search? A historical or current failure to keep visible is **Groq quota 429**. Current live evidence to incorporate is: Groq returned HTTP 429 across the configured candidate models during the same audit.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## 2.38 Plan maintenance — causal implementation review

At the end of every live validation cycle, append what was learned, what was disproved, which assumptions changed and which implementation tasks remain. The 40k plan is intentionally mutable; new reality outranks old prose. This review must begin with the actual model-visible assignment and end at the durable outcome. Do not infer the cause from the final string alone. Trace the objective, observations, model action, executor result, model interpretation, candidate/evidence parser, persistence, promotion and telemetry. The review must answer: Did the model pivot because of evidence or because deterministic code told it where to go? A historical or current failure to keep visible is **Mistral secret absent**. Current live evidence to incorporate is: Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.

The implementation rule is that uncertainty is valid. If evidence does not establish a person, relationship or contact route, the agent may continue, pivot, or stop without a candidate. It must never invent a target merely to satisfy a targetCount. If a provider is unavailable, the runtime must preserve the research objective while failing over or stopping honestly; it must never convert provider failure into a deterministic research playbook. If a source is a list, generic page, inaccessible page, title fragment, address, product, department or organization-only surface, the system must preserve its type rather than forcing it into a person-shaped slot.

Testing for this section requires a local unit invariant, a typed integration fixture, an observable telemetry assertion and, when credentials permit, a live or replayable end-to-end case. A passing static test proves only the local property it covers. A live claim requires a real provider-backed trajectory. A comparison claim requires an independent researcher on the same target and objective. Any test that can pass while the model never executes a real decision is insufficient for research-quality claims.

The engineering review must record false positives. A string resembling a full name is not enough; the source must bind that name to the candidate context. An email is not enough; its scope must be attributable. High net worth is not enough; it does not establish practical reachability. A green provider count is not enough; a successful model response is required. A tool span is not enough; the action must be attributable to the model rather than a hidden scripted fallback.

## Final acceptance matrix

1. **Acceptance check 1:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

2. **Acceptance check 2:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

3. **Acceptance check 3:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

4. **Acceptance check 4:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

5. **Acceptance check 5:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

6. **Acceptance check 6:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

7. **Acceptance check 7:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

8. **Acceptance check 8:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

9. **Acceptance check 9:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

10. **Acceptance check 10:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

11. **Acceptance check 11:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

12. **Acceptance check 12:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

13. **Acceptance check 13:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

14. **Acceptance check 14:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

15. **Acceptance check 15:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

16. **Acceptance check 16:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

17. **Acceptance check 17:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

18. **Acceptance check 18:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

19. **Acceptance check 19:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

20. **Acceptance check 20:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

21. **Acceptance check 21:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

22. **Acceptance check 22:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

23. **Acceptance check 23:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

24. **Acceptance check 24:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

25. **Acceptance check 25:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

26. **Acceptance check 26:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

27. **Acceptance check 27:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

28. **Acceptance check 28:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

29. **Acceptance check 29:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

30. **Acceptance check 30:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

31. **Acceptance check 31:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

32. **Acceptance check 32:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

33. **Acceptance check 33:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

34. **Acceptance check 34:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

35. **Acceptance check 35:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

36. **Acceptance check 36:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

37. **Acceptance check 37:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

38. **Acceptance check 38:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

39. **Acceptance check 39:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

40. **Acceptance check 40:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

41. **Acceptance check 41:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

42. **Acceptance check 42:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

43. **Acceptance check 43:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

44. **Acceptance check 44:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

45. **Acceptance check 45:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

46. **Acceptance check 46:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

47. **Acceptance check 47:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

48. **Acceptance check 48:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

49. **Acceptance check 49:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

50. **Acceptance check 50:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

51. **Acceptance check 51:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

52. **Acceptance check 52:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

53. **Acceptance check 53:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

54. **Acceptance check 54:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

55. **Acceptance check 55:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

56. **Acceptance check 56:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

57. **Acceptance check 57:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

58. **Acceptance check 58:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

59. **Acceptance check 59:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

60. **Acceptance check 60:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

61. **Acceptance check 61:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

62. **Acceptance check 62:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

63. **Acceptance check 63:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

64. **Acceptance check 64:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

65. **Acceptance check 65:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

66. **Acceptance check 66:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

67. **Acceptance check 67:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

68. **Acceptance check 68:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

69. **Acceptance check 69:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

70. **Acceptance check 70:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

71. **Acceptance check 71:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

72. **Acceptance check 72:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

73. **Acceptance check 73:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

74. **Acceptance check 74:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

75. **Acceptance check 75:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

76. **Acceptance check 76:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

77. **Acceptance check 77:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

78. **Acceptance check 78:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

79. **Acceptance check 79:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

80. **Acceptance check 80:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

81. **Acceptance check 81:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

82. **Acceptance check 82:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

83. **Acceptance check 83:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

84. **Acceptance check 84:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

85. **Acceptance check 85:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

86. **Acceptance check 86:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

87. **Acceptance check 87:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

88. **Acceptance check 88:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

89. **Acceptance check 89:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

90. **Acceptance check 90:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

91. **Acceptance check 91:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

92. **Acceptance check 92:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

93. **Acceptance check 93:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

94. **Acceptance check 94:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

95. **Acceptance check 95:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

96. **Acceptance check 96:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

97. **Acceptance check 97:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

98. **Acceptance check 98:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

99. **Acceptance check 99:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

100. **Acceptance check 100:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

101. **Acceptance check 101:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

102. **Acceptance check 102:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

103. **Acceptance check 103:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

104. **Acceptance check 104:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

105. **Acceptance check 105:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

106. **Acceptance check 106:** replay or synthesize a realistic observation containing **President PERSON** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

107. **Acceptance check 107:** replay or synthesize a realistic observation containing **State St** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

108. **Acceptance check 108:** replay or synthesize a realistic observation containing **Operational Enablement** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

109. **Acceptance check 109:** replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

110. **Acceptance check 110:** replay or synthesize a realistic observation containing **security issues** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

111. **Acceptance check 111:** replay or synthesize a realistic observation containing **Forbes billionaire list** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

112. **Acceptance check 112:** replay or synthesize a realistic observation containing **generic contact form** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

113. **Acceptance check 113:** replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Gemini 3.7 Flash returned HTTP 429 with an explicit free-tier generate-content quota exhaustion message and retry delay.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

114. **Acceptance check 114:** replay or synthesize a realistic observation containing **job title promoted as person** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Groq returned HTTP 429 across the configured candidate models during the same audit.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

115. **Acceptance check 115:** replay or synthesize a realistic observation containing **NVIDIA hosted model 410** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **NVIDIA returned HTTP 410 for the selected hosted model aliases, exposing a provider lifecycle mismatch rather than a research-quality failure.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

116. **Acceptance check 116:** replay or synthesize a realistic observation containing **Gemini quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Mistral was absent from the GitHub Actions environment, so it was not actually available for fallback.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

117. **Acceptance check 117:** replay or synthesize a realistic observation containing **Groq quota 429** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **Static free-ReAct, trajectory and discovery-quality checks passed, proving that static autonomy assertions cannot substitute for provider-backed execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

118. **Acceptance check 118:** replay or synthesize a realistic observation containing **Mistral secret absent** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The audit workflow previously changed source before building it; permanent source changes are now committed before the tested build.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

119. **Acceptance check 119:** replay or synthesize a realistic observation containing **zero candidates after broad search** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The first provider-circuit implementation was not idempotent and produced duplicate declarations; this is now itself a regression class that the patcher must eliminate on repeated execution.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.

120. **Acceptance check 120:** replay or synthesize a realistic observation containing **com EMAIL** and prove the causal boundary. Establish whether a real provider-backed model decision occurred, preserve exact source provenance, keep person and organization scope separate, prevent unsupported identity promotion, and show that the next research action remained model-selected. Incorporate the current evidence that **The latest live audit produced zero candidates because every configured LLM attempt failed; the run was correctly classified as bureauIntegrity=critical.**. Record pass, fail, or inconclusive; never turn provider outage into a research-quality win or loss. If the test fails, preserve the exact trajectory and add a regression artifact before declaring the repair complete.



**Generated word count:** 43794.

# Apex Atlas — 40,000+ Word Research-Agent Integrity, Identity, Evidence, and Live-Comparison Plan

Generated as an executable engineering contract. The minimum is 40000 words; the generator fails closed if the resulting document is shorter.

## Purpose

This plan exists because repeated commits must not merely move symptoms around. The system must be debugged as a causal research-agent stack. The standard is behavioral: Apex should make the same kind of sensible research decisions a strong independent LLM researcher would make when given the same objective, while having substantially better tool access, persistence, provenance and repeatability. Deterministic code may protect truth and system integrity, but it must not secretly become the researcher.

## 1.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 1.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 2.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 3.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 4.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 5.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 6.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 7.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 8.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 9.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 10.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 11.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 12.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 13.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 14.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 15.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 16.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 17.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 18.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 19.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 20.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 21.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 22.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 23.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 24.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 25.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 26.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 27.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 28.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 29.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 30.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 31.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 32.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 33.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 34.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 35.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 36.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 37.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 38.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 39.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 40.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 41.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 42.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 43.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 44.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 45.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 46.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 47.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 48.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 49.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 50.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 51.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 52.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 53.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 54.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 55.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 56.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 57.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 58.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 59.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 60.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 61.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 62.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 63.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 64.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 65.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 66.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 67.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 68.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 69.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.1 Executive mandate — causal review

Define the non-negotiable goal: Apex must behave as a capable research agent, with model judgment preserved and deterministic software limited to safety, provenance, lifecycle, budgets, persistence and promotion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.2 Causal failure taxonomy — causal review

Separate model judgment failures from prompt failures, observation corruption, tool failures, action parsing, extraction, identity binding, persistence, promotion, telemetry, provider and runtime failures. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.3 Observation contract — causal review

Every model turn receives typed observations with source URL, retrieval status, page identity, content boundaries, tool provenance and timestamps; heterogeneous fields are never silently flattened. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.4 Action contract — causal review

The model owns the next action. Validators may reject malformed or unauthorized actions, but may not select queries, sources, targets, rankings, hop order or recovery strategy. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.5 Identity contract — causal review

A person is an attributable identity claim, not a token. Require a plausible full name, candidate scope, exact HTTP(S) provenance and evidence that binds that name to the source. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.6 Evidence graph — causal review

Keep claims, sources, observations, entities and contact surfaces typed and linked. Organization evidence cannot silently become personal evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.7 Discovery agent — causal review

Search results are leads. The model decides what to inspect, pivot, corroborate and when to stop. Discovery must optimize for practical research value rather than fame. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.8 Target-contact agent — causal review

For a known person, the model independently selects searches, visits and OSINT tools to find attributable public or intermediary routes. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.9 Practical reachability — causal review

Prefer realistic operating-company, professional, adviser, representative and intermediary surfaces over celebrity or billionaire enumeration. Fame is not reachability. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.10 Contact semantics — causal review

Separate personal email, personal phone, professional profile, organization inbox, organization phone, representative and unknown. Never upgrade scope without evidence. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.11 Promotion integrity — causal review

Only evidence-backed claims reach cards. Rehydration must be deterministic mapping, never synthetic completion. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.12 Model roles — causal review

Boss and right-hand provide direction and review without scripting the research path. Failover preserves state and semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a search result be mistaken for evidence without inspection? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.13 Provider behavior — causal review

Track provider availability, rate limits and degradation without allowing provider failure to mutate the research objective into a scripted fallback. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a failed provider cause a scripted recovery path? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.14 Timeout architecture — causal review

Use cancellation, checkpoints, bounded retries and resumable state. A timeout must not cause a hidden alternate playbook or repeated expensive loop. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can a timeout duplicate work or mutate state? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.15 Redis discipline — causal review

Bound key cardinality, payload size, write frequency and TTLs. Instrument every expensive write and distinguish application churn from provider usage. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can persistence change the meaning of the model's claim? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.16 Persistence integrity — causal review

Database writes must retain raw claim provenance and typed scope so later UI code cannot reconstruct identity from ambiguous strings. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can promotion invent a value that was never evidenced? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.17 Telemetry integrity — causal review

Audit model action, tool execution, observation, interpretation, claim, validation, persistence and promotion as separate events. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can telemetry prove what the model actually selected? A historical regression to guard against is **Forbes billionaire list**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.18 Adversarial fixtures — causal review

Use the exact historical failures: com EMAIL, President PERSON, State St, Operational Enablement, Product Comparisons Sage Products, security issues and fame-list artifacts. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an independent researcher reproduce the same observation and reach a different conclusion? A historical regression to guard against is **generic contact form**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.19 Model-human differential test — causal review

Feed the same evidence to an independent researcher and compare whether the defect originates in observation, model reasoning or downstream transformation. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Does the system prefer realistic reachability over fame? A historical regression to guard against is **company inbox promoted as personal**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.20 Ten-target batch protocol — causal review

Freeze ten real targets, run Apex independently, run an independent researcher on the same targets, then compare route quality, identity, provenance and efficiency. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What regression test would fail if this defect returned? A historical regression to guard against is **job title promoted as person**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.21 Regression architecture — causal review

Every discovered failure becomes a fixture, invariant, telemetry assertion or end-to-end test. No blacklist-only fixes are accepted as complete repairs. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What input does the model actually see? A historical regression to guard against is **com EMAIL**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.22 Live execution — causal review

A green build is not a quality result. Require a real provider-backed run, complete trajectory, database state, source URLs and independent comparison. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: What exact source and URL produced the observation? A historical regression to guard against is **President PERSON**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.23 Security and secrets — causal review

Secrets remain runtime-only, never appear in source or telemetry, and provider checks expose status without values. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Could a field label or HTML fragment be mistaken for an identity? A historical regression to guard against is **State St**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.24 Replit parity — causal review

The project runtime must execute the same built code and contracts as CI. Environment recovery must not alter application semantics. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can the model distinguish a person from an organization? A historical regression to guard against is **Operational Enablement**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.25 Acceptance gates — causal review

Define hard gates for identity correctness, source binding, free-ReAct autonomy, contact scope, promotion honesty, Redis behavior and comparison quality. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Can an organization contact remain organization-scoped? A historical regression to guard against is **Product Comparisons Sage Products**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## 70.26 Implementation sequence — causal review

Make causal fixes in small commits, build and test after each high-risk boundary, then conduct live batches and independent comparisons. The review for this section must trace the complete boundary rather than inspect only the final string. Start from the model-visible objective and observation, identify the action selected by the model, record the exact tool result, then follow the interpretation and persistence path until the final ledger/card representation. The investigation must answer: Is the model choosing the action or is deterministic code choosing it? A historical regression to guard against is **security issues**. The repair is not complete if it merely adds another literal rejection rule; the underlying representation, schema, prompt, parser, persistence or promotion boundary must make the bad state difficult or impossible to create.

The engineering rule for this review is that uncertainty is a valid terminal state. If the evidence does not establish a real person, relationship or contact route, Apex must preserve that uncertainty instead of filling a required field with a nearby token. If a source is generic, list-only, inaccessible or unrelated, the agent may pivot, inspect another lead, or stop. What it may not do is manufacture a target to satisfy a batch quota. Likewise, if a provider fails, the system may fail over according to the documented provider mechanism, but failover must preserve the same research assignment and must not substitute a deterministic search plan.

Testing for this section must include a unit boundary test, a typed integration fixture, a telemetry assertion and at least one end-to-end regression. The unit test should isolate the local invariant. The integration fixture should carry realistic heterogeneous web observations. The telemetry assertion should prove which actor made the decision. The end-to-end regression should verify that the malformed state cannot reach persistence or promotion. Every test should report the causal stage of failure so a future green test suite cannot conceal a broken audit surface.

## Final acceptance matrix

1. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

2. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

3. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

4. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

5. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

6. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

7. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

8. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

9. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

10. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

11. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

12. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

13. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

14. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

15. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

16. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

17. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

18. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

19. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

20. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

21. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

22. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

23. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

24. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

25. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

26. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

27. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

28. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

29. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

30. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

31. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

32. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

33. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

34. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

35. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

36. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

37. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

38. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

39. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

40. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

41. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

42. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

43. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

44. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

45. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

46. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

47. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

48. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

49. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

50. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

51. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

52. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

53. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

54. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

55. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

56. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

57. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

58. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

59. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

60. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

61. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

62. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

63. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

64. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

65. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

66. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

67. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

68. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

69. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

70. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

71. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

72. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

73. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

74. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

75. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

76. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

77. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

78. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

79. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

80. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

81. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

82. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

83. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

84. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

85. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

86. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

87. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

88. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

89. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

90. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

91. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

92. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

93. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

94. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

95. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

96. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

97. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

98. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

99. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

100. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

101. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

102. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

103. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

104. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

105. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

106. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

107. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

108. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

109. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

110. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

111. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

112. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

113. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

114. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

115. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

116. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

117. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

118. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

119. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

120. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

121. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

122. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

123. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

124. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

125. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

126. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

127. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

128. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

129. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

130. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

131. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

132. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

133. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

134. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

135. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

136. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

137. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

138. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

139. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

140. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

141. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

142. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

143. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

144. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

145. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

146. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

147. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

148. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

149. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

150. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

151. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

152. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

153. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

154. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

155. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

156. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

157. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

158. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

159. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

160. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

161. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

162. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

163. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

164. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

165. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

166. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

167. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

168. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

169. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

170. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

171. Acceptance check: replay or synthesize a realistic observation containing **President PERSON** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

172. Acceptance check: replay or synthesize a realistic observation containing **State St** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

173. Acceptance check: replay or synthesize a realistic observation containing **Operational Enablement** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

174. Acceptance check: replay or synthesize a realistic observation containing **Product Comparisons Sage Products** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

175. Acceptance check: replay or synthesize a realistic observation containing **security issues** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

176. Acceptance check: replay or synthesize a realistic observation containing **Forbes billionaire list** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

177. Acceptance check: replay or synthesize a realistic observation containing **generic contact form** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

178. Acceptance check: replay or synthesize a realistic observation containing **company inbox promoted as personal** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

179. Acceptance check: replay or synthesize a realistic observation containing **job title promoted as person** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.

180. Acceptance check: replay or synthesize a realistic observation containing **com EMAIL** and prove that Apex either identifies a genuinely attributable person with exact provenance or explicitly declines admission. Prove that the model retains control over subsequent research actions; that organization and personal contact scopes remain distinct; that no unsupported value reaches the card; and that the telemetry makes the complete causal path auditable. A passing check must include the test name, expected invariant, observed result, and regression coverage.



**Word count:** 602548.

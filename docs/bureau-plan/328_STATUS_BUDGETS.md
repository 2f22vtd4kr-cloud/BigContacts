# Volume 328 — Status Plane Budgets

atlas-status and healthz must use short Redis budgets and event-loop yields during dig so the desk stays operable. Worker-thread dig remains a later isolation tier if yields insufficient.

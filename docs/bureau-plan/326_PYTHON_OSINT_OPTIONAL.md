# Volume 326 — Python OSINT Optional at Boot

Holehe/Maigret/Sherlock are **tools** the dig may call. Boot may skip install when `INSTALL_PYTHON_OSINT=false`, but then footprint actions must fail honestly in observations—not crash the job.

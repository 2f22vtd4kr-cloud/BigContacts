You are continuing work on the existing Apex Atlas / BigContacts repository.

DO NOT restart the project, replace the architecture, create a parallel app, or reopen already-passed static work without evidence.

CURRENT VERIFIED STATE

The following already passed and must be preserved:

- Full workspace TypeScript typecheck
- API build
- Desk build
- Database schema push
- check:no-force-dig
- check:free-react
- Agentic runtime and canonical promotion checks
- API health
- Redis health
- bureauIntegrity
- Desk at /
- API under /api
- Canonical source/build hardening and idempotence
- Discovery-first runtime proof

A bounded discovery run admitted a real source-backed entity:

- Entity ID: 1
- Name: Darcy Morris
- Source: https://ewingmorris.com/team-member/darcy-morris-2/

DO NOT fake or script research results.

REMAINING ACCEPTANCE FAILURES

1. A standard single-target Dig did not fully complete.

Evidence:

- Job: 9989b269-495e-4361-96a1-0e2ff84ae99a
- terminal lifecycle: done
- outcome: incomplete
- target result: timeout_review
- error: target enrichment exceeded configured 180s timeout

2. Scoreboard proof remains:

- count: 0
- mean: 0
- milestonePass: false

The current entity remains uncooked because the Dig did not reach the canonical completed research transition.

3. GitHub push failed:

remote: Invalid username or token.
fatal: Authentication failed

The working tree is clean and the branch contains local commits ahead of origin/main. Do not recreate or squash those commits unnecessarily.

PRIMARY OBJECTIVE

Finish the remaining acceptance gates honestly:

A. Diagnose and repair the Dig timeout.
B. Produce a real completed single-target Dig through the canonical research pipeline.
C. Produce a legitimate non-zero scoreboard row and milestonePass evidence through canonical completion logic.
D. Verify all previous static and runtime gates still pass.
E. Push existing local commits plus any new legitimate fixes once GitHub authentication is correctly configured.
F. Only after acceptance passes, publish/deploy if deployment is requested and supported.

DIG TIMEOUT INVESTIGATION RULES

Do not solve this by merely increasing the 180-second timeout.

Trace the actual target enrichment lifecycle and determine:

- exact stage(s) consuming the timeout;
- provider calls made;
- retries;
- Retry-After handling;
- provider/account cooldown interaction;
- concurrency behavior;
- serial versus parallel execution;
- whether partial evidence is persisted progressively;
- whether quota controls cause avoidable waiting;
- whether a provider continues consuming time after the Dig has enough evidence to finish.

Preserve:

- honest research behavior;
- model-selected research actions;
- no forced provider actions;
- no fabricated identities;
- no hardcoded target-specific success;
- no fake contacts;
- no fake source evidence.

If a timeout architecture issue exists, repair the shared contract so normal standard Digs can finish under normal provider conditions while genuinely slow/exhausted research still terminates honestly.

SCOREBOARD RULES

Do not manually mutate cookedAt, score values, or database rows merely to manufacture a passing result.

Trace the canonical path:

research evidence
-> target completion
-> legitimate cooked/completed transition
-> scoreboard inclusion
-> scoring
-> milestone calculation

Repair a broken transition if one exists.

Then run a real workflow until the API produces evidence such as:

- at least one legitimate scored row;
- non-zero count;
- actual mean;
- milestonePass=true if the existing acceptance threshold is met.

If milestonePass cannot legitimately pass, report the precise remaining product constraint instead of faking it.

EXTERNAL QUOTA CONTROLS

Preserve and verify the shared controls for all quota-limited external services, not just Redis.

Review real execution paths for:

- LLM providers
- search providers
- scraping providers
- browser/visit/fetch providers
- registries or external data APIs
- Redis where applicable

Verify:

- bounded concurrency;
- per-provider/account cooldowns;
- correct 429 handling;
- Retry-After support;
- bounded retries;
- no retry storms;
- no repeated calls after quota exhaustion;
- normal fallback behavior where architecture supports it.

Do not add artificial throttling that makes a normal Dig more likely to hit its global deadline.

GITHUB

Diagnose the authentication failure without exposing secrets.

Check:

git remote -v
git status -sb
git log --oneline origin/main..HEAD
git ls-remote origin

Determine whether the issue is:

- missing credential;
- invalid/expired PAT;
- insufficient PAT permissions;
- cached wrong credentials;
- wrong remote repository/account.

Do not print tokens.

Once authentication works, push the existing commits and any legitimate new commits normally. No force push.

FINAL VERIFICATION ORDER

1. git diff --check
2. clean/idempotent canonical-source checks
3. pnpm run typecheck
4. API build
5. Desk build
6. required static guards
7. database schema verification
8. API startup
9. /api/healthz
10. Redis and bureauIntegrity
11. root desk /
12. real discovery proof if needed
13. real standard single-target Dig
14. wait for genuine terminal completion
15. inspect entity completion state
16. inspect contacts/source evidence
17. run scoreboard check
18. verify non-zero legitimate rows
19. verify milestone result
20. git status
21. commit only actual source changes
22. push normally after authentication is fixed

FINAL REPORT FORMAT

Report separately:

PASSED
- exact command/check
- concise result

FIXED
- root cause
- files/contracts changed
- why the repair is architecture-safe

LIVE EVIDENCE
- discovery job
- entity ID
- Dig job
- terminal result
- source evidence
- contact evidence if legitimately found
- scoreboard count
- scoreboard mean
- milestonePass

BLOCKERS
- only genuine unresolved blockers

GIT
- commit IDs
- push result
- exact remote branch status

Do not claim full acceptance unless the Dig and scoreboard requirements genuinely pass.
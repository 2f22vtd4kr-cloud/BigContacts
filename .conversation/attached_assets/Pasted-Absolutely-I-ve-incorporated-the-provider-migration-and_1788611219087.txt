Absolutely. I’ve incorporated the provider migration and the audit fixes: Gemini = Boss, DeepSeek-V4-Flash-0731 via NVIDIA Integrate = Right Hand, Groq → Mistral = Dig, the hardened migration script, stricter stale-provider checks, and the requirement that DeepSeek never leaks into the Dig lane.

Apex Atlas — execute end-to-end in THIS Replit App only. One report at the end. No redesign. END when done.

═══════════════════════════════════════
IDENTITY
═══════════════════════════════════════
Repo: https://github.com/2f22vtd4kr-cloud/BigContacts
Branch: main
Tip floor: latest main / Batch 50+ live handoff
Product: AI free-ReAct OSINT desk — real public contact routes to HNWIs / principals / operators.

LAW:
• Models invent queries and choose tools; tools execute.
• NEVER force_* hops.
• NEVER GROK-PARITY.
• NEVER use prefer-lists.
• NEVER use scripted Dig playbooks.
• NEVER hard-code a research trajectory.
• NEVER invent people, contacts, companies, evidence, or URLs.
• Deterministic runtime code may enforce authorization, lifecycle, provenance, validation, budgets, timeouts, safety, promotion, and integrity.
• Deterministic code must NOT choose the research path for the Dig investigator.
• Boss and Right Hand may reason, advise, summarize, and critique.
• Boss and Right Hand do NOT browse, execute OSINT, or choose Dig hops.
• Dig Investigator chooses search / visit / OSINT / pivots / stop.
• Forbes-style targets are unrealistic for HNWI-level contact research. Treat them as degraded, not as a win.

MODEL ROLES — NON-NEGOTIABLE:
• BOSS = Gemini
• RIGHT HAND / ADVISOR = DeepSeek-V4-Flash-0731 via NVIDIA Integrate
• DIG INVESTIGATOR = Groq → Mistral failover
• TOOLS = model-selected tool executors
• ORCHESTRATOR = deterministic lifecycle runtime

DeepSeek is an ADVISOR only.
DeepSeek MUST NOT become a Dig provider.
Groq → Mistral MUST remain the Dig investigator failover chain.

═══════════════════════════════════════
PLATFORM — REPLIT AUG 2026+
═══════════════════════════════════════
• This is an existing Replit App / project.
• Do NOT say “Repl”.
• Do NOT create a second app.
• Do NOT redesign or replace Apex Atlas.
• Work inside THIS existing App and repository only.
• Do not ask the operator to create another Replit project.
• Do not ask the operator to register for another external service.

POSTGRES:
• Postgres is platform-provided.
• Replit injects DATABASE_URL automatically.
• NEVER ask for DATABASE_URL.
• NEVER ask the operator to paste, attach, or create DATABASE_URL.
• Use the existing Replit platform Postgres connection.

REDIS:
• Redis is operator-supplied through Upstash.
• Canonical operator secret = REDIS_URL_1.
• NEVER ask for REDIS_URL.
• NEVER ask for REDIS_URL_2, REDIS_URL_3, REDIS_URL_4, or REDIS_URL_5.
• Existing boot compatibility may alias REDIS_URL_1 → REDIS_URL internally.
• Never print Redis secret values.

═══════════════════════════════════════
HARD RULES
═══════════════════════════════════════
• API Server workflow ONLY on PORT=8080.
• Do NOT start Frontend / separate Vite preview as the product.
• The public product is served by the API workflow.
• ENABLE_AUTO_PIPELINE=false unless the operator explicitly says otherwise.
• NEVER ask for WHOXY credentials.
• NEVER ask for additional Redis keys.
• NEVER print secret values.
• NEVER expose environment-variable values in the final report.
• NEVER invent entities, people, contacts, evidence, URLs, or search results.
• Scoreboard proof = single-target Dig at standard depth.
• Discovery is only a seed when the ledger is empty.
• Do NOT use discovery-first bulk research as scoreboard proof.
• If bureauIntegrity=critical → report exact blocker and END.
• If Redis is down or quota exhausted → operator replaces REDIS_URL_1 offline, restart API, re-check healthz.
• Live audit must catch and stop any scripted Phase 0 cross-reference or junk target before it becomes an entity.
• typecheck MUST pass.
• Fix module/type errors before claiming build OK.
• Do not call a build successful merely because another build succeeded.
• Do not silently downgrade a failing provider to an obsolete provider.
• Do not fall back to z.ai / GLM / old NVIDIA NIM / Nemotron for the Right Hand.
• If the DeepSeek migration verification fails, STOP before claiming provider migration succeeded.
• Do not modify historical .conversation or docs/archive records merely to erase history.
• Historical references are not evidence that the active runtime is still using the old provider.

═══════════════════════════════════════
STEP 0 — VERIFY TIP
═══════════════════════════════════════
From the workspace root:

git fetch origin main
git checkout main
git pull origin main
git log -1 –oneline
git remote -v

Verify the current HEAD is the latest available origin/main.

Also verify the minimum floor:

git rev-parse HEAD

If the repository is older than:

c2a8b93ff9e9743af3935d9563464952c656e43a

STOP immediately and report the SHA.

Do not continue against an older branch.

═══════════════════════════════════════
STEP 1 — WORKFLOW ENVIRONMENT
═══════════════════════════════════════
These are WORKFLOW ENVIRONMENT variables, not secrets:

PORT=8080
ENABLE_AUTO_PIPELINE=false
INSTALL_PYTHON_OSINT=false
APEX_SKIP_SEMANTIC=1
CI=true
RESEARCH_DEPTH=standard
NODE_OPTIONS=–max-old-space-size=1536

The canonical Replit workflow must use:

bash scripts/replit-boot.sh

from the repository/workspace root.

Do NOT start:

• artifacts/apex-finder as a standalone product
• a separate Vite preview
• another frontend workflow
• another API workflow
• a second application

═══════════════════════════════════════
STEP 2 — SECRETS
═══════════════════════════════════════
The operator must configure secrets in Replit Secrets.

NEVER print their values.

REQUIRED FOR THE INTENDED THREE-LANE ARCHITECTURE:

REDIS_URL_1
GEMINI_API_KEY
DEEPSEEK_API_KEY
GROQ_API_KEY
MISTRAL_API_KEY
SERPER_API_KEY OR TAVILY_API_KEY OR EXA_API_KEY

Provider roles:

GEMINI_API_KEY
→ Boss

DEEPSEEK_API_KEY
→ Right Hand / Advisor
→ NVIDIA Integrate transport
→ DeepSeek-V4-Flash-0731

GROQ_API_KEY
→ Dig Investigator primary

MISTRAL_API_KEY
→ Dig Investigator failover

SERPER_API_KEY / TAVILY_API_KEY / EXA_API_KEY
→ Search tool surface

FULL OPTIONAL/AVAILABLE SECRET INVENTORY:

REDIS_URL_1
GEMINI_API_KEY
DEEPSEEK_API_KEY
GROQ_API_KEY
MISTRAL_API_KEY
HF_TOKEN
SERPER_API_KEY
TAVILY_API_KEY
SERPAPI_KEY
EXA_API_KEY
SCRAPFLY_API_KEY
ZENROWS_API_KEY
COMPANIES_HOUSE_API_KEY
WHOISJSON_API_KEY

IMPORTANT:
• Do NOT request NVIDIA_NIM_API_KEY.
• Do NOT request NVIDIA_NIM_MODEL.
• Do NOT request z.ai / ZAI / GLM credentials.
• Do NOT request WHOXY credentials.
• Do NOT request REDIS_URL or REDIS_URL_2–REDIS_URL_5.
• Do NOT request DATABASE_URL.
• Do NOT invent missing secret values.
• Do NOT print secret values.

If a required secret is missing, report its NAME only and STOP rather than inventing a fallback.

Do not substitute DeepSeek with an old NVIDIA NIM model.

═══════════════════════════════════════
STEP 3 — MIGRATE RIGHT-HAND PROVIDER BEFORE BUILD
═══════════════════════════════════════
Before building or starting the API, execute the repository’s hardened migration:

node scripts/migrate-right-hand-to-deepseek.mjs

This migration is intentionally idempotent and restricted to the audited active runtime/documentation allowlist.

It MUST establish:

RIGHT HAND:
DeepSeek-V4-Flash-0731

MODEL:
deepseek-ai/deepseek-v4-flash-0731

TRANSPORT:
NVIDIA Integrate

ENDPOINT:
https://integrate.api.nvidia.com/v1/chat/completions

CREDENTIAL:
DEEPSEEK_API_KEY

REQUIRED GENERATION CONTRACT:

temperature: 1
top_p: 0.95
max_tokens: 16384

extra_body:
{
chat_template_kwargs: {
thinking: true,
reasoning_effort: “high”
}
}

stream:
false

Reasoning extraction must support the NVIDIA response fields:

message.reasoning

and/or

message.reasoning_content

Do NOT replace the NVIDIA Integrate transport with another provider.

Do NOT convert DeepSeek into a Dig provider.

Do NOT reduce the requested max_tokens contract.

Do NOT silently remove reasoning mode.

Do NOT create duplicate temperature, top_p, max_tokens, or extra_body parameters.

After migration, verify the migration script itself reports success.

If migration verification fails:
STOP.
Do not build.
Do not start the server.
Report the exact error.

═══════════════════════════════════════
STEP 4 — VERIFY PROVIDER ROLE ISOLATION
═══════════════════════════════════════
Run the repository’s provider-role documentation/runtime guard if present.

Before invoking ANY named pnpm run check:* script:
inspect package.json and confirm that exact script exists.

Never invent a package script.

The active architecture MUST remain:

BOSS
Gemini

RIGHT HAND
DeepSeek-V4-Flash-0731 via NVIDIA Integrate

DIG
Groq → Mistral

Verify that active runtime code does NOT route:

DeepSeek → Dig
Gemini → Dig
old NVIDIA NIM → Right Hand
z.ai / GLM → Right Hand

Verify Dig hardening still contains the canonical:

DIG_INVESTIGATOR_FAILOVER_CHAIN

with:

Groq → Mistral

Do not weaken or remove the Dig hardening.

═══════════════════════════════════════
STEP 5 — STALE ACTIVE-RUNTIME PROVIDER CHECK
═══════════════════════════════════════
Search the ACTIVE runtime/config/documentation surfaces for stale provider identifiers.

Check for:

NVIDIA_NIM_API_KEY
NVIDIA_NIM_MODEL
NVIDIA_AGENTIC_MODEL
nvidia/nemotron-3-nano-30b-a3b
nvidia-right-hand
nvidia-nim
z-AI
z.ai
GLM

IMPORTANT:
Historical .conversation and docs/archive content may contain old architecture references. Do NOT rewrite history merely to make a grep clean.

The following MUST NOT remain as the active Right Hand implementation.

The active Right Hand MUST contain:

DEEPSEEK_API_KEY
deepseek-ai/deepseek-v4-flash-0731
https://integrate.api.nvidia.com/v1/chat/completions
reasoning_effort: “high”
thinking: true

If active runtime still resolves the Right Hand to an obsolete provider:
STOP and fix it before build.

═══════════════════════════════════════
STEP 6 — INSTALL — OOM + FIREWALL SAFE
═══════════════════════════════════════
From workspace root:

export NODE_OPTIONS=–max-old-space-size=1536
export NPM_CONFIG_REGISTRY=https://registry.npmjs.org

pnpm config set registry https://registry.npmjs.org
pnpm config set network-timeout 600000

If pnpm-lock.yaml contains tarball URLs pointing to an internal/firewall proxy such as:

http://35.245.43.102/npm/…

rewrite ONLY those tarball hosts to:

https://registry.npmjs.org/

Keep:

• package names
• versions
• integrity hashes

Do not remove dependencies.

Then:

pnpm install –no-frozen-lockfile –registry=https://registry.npmjs.org 
–child-concurrency 1 
–network-concurrency 1 
–fetch-retries 5 
–fetch-timeout 600000

If exit code 137:
retry ONCE with the exact same flags.

If the lockfile was accidentally emptied/corrupted:
restore the latest valid lockfile from HEAD:

git checkout HEAD – pnpm-lock.yaml

Then rerun the install.

Do NOT strip dependencies to make installation pass.

After a successful public-registry installation:
DO NOT restore an obsolete firewall-proxy lockfile.

Install must finish successfully before continuing.

═══════════════════════════════════════
STEP 7 — DB + DESK + API BUILDS
═══════════════════════════════════════
Use the platform-provided DATABASE_URL automatically.

Run:

pnpm –filter @workspace/db run push

If DB push fails:
use the existing Replit/platform Postgres database tooling/path.
Do NOT ask the operator for DATABASE_URL.

Then:

pnpm –dir artifacts/apex-finder run build

Verify:

test -f artifacts/apex-finder/dist/public/index.html

If missing:

echo DESK_BUILD_FAILED
exit 1

Then:

pnpm –dir artifacts/api-server run build

Then:

pnpm run typecheck

TYPECHECK IS MANDATORY.

If typecheck fails:
fix the actual module/type errors.
Do NOT claim the build is good.
Do NOT continue to runtime proof until typecheck passes.

Then inspect package.json.

If these scripts exist, run them:

pnpm run check:no-force-dig
pnpm run check:free-react

If provider-role / trajectory / comparison / integrity checks exist, run the exact existing scripts as well.

Never invent missing scripts.

═══════════════════════════════════════
STEP 8 — BOOT THE CANONICAL REPLIT WORKFLOW
═══════════════════════════════════════
The only product workflow is:

ENABLE_AUTO_PIPELINE=false 
RESEARCH_DEPTH=standard 
PORT=8080 
bash scripts/replit-boot.sh

The boot script is responsible for the existing Apex Atlas startup sequence.

Do NOT manually start another frontend.

Do NOT run a second API server.

Do NOT create another workflow.

If the API is already running under the canonical workflow, do not launch a duplicate process.

═══════════════════════════════════════
STEP 9 — HEALTH CHECK
═══════════════════════════════════════
Run:

curl -sS http://127.0.0.1:8080/api/healthz

Report only these health dimensions:

status
redis
bureauIntegrity

Do NOT print secrets.

Expected:

status = healthy/OK equivalent
redis = connected
bureauIntegrity != critical

If:

bureauIntegrity=critical

STOP.

Report the exact non-secret reason and END.

Do not attempt to manufacture a scoreboard pass.

If Redis is:

• down
• unavailable
• quota exhausted

STOP the research run.

Operator action is offline:

1. Replace REDIS_URL_1 in Replit Secrets.
2. Restart the API Server workflow.
3. Re-run /api/healthz.

Do not ask for REDIS_URL_2–5.

═══════════════════════════════════════
STEP 10 — PUBLIC DESK PREVIEW
═══════════════════════════════════════
Open THIS Replit App’s public URL at:

/

Hard refresh.

Confirm the current Apex Atlas desk is non-blank.

Verify visible/current product surfaces include:

• Entities
• Profile
• Reactor
• Dig contacts / evidence surface

Do NOT treat old “ApexFinder Pro” artifacts as the current product.

Do NOT redesign the UI.

Do NOT replace the desk because a visual artifact looks old.

Only fix a genuine runtime/build failure.

═══════════════════════════════════════
STEP 11 — LEDGER CHECK
═══════════════════════════════════════
Run:

GET /api/entities?limit=5

If at least one legitimate entity exists:
DO NOT start discovery.

Proceed directly to Step 12.

If the ledger is empty:
run ONE tiny discovery-first seed only.

POST /api/ingest/atlas-run

Content-Type: application/json

{
“discoveryFirst”: true,
“targetCount”: 3,
“researchLimit”: 3,
“runResearch”: true,
“skipFaa”: true,
“broadCategories”: 1,
“batchSize”: 10,
“phaseJBatchSize”: 5,
“targetTimeoutMs”: 180000,
“researchDepth”: “standard”
}

This is a SEED ONLY.

Discovery MUST NOT become the scoreboard proof.

Poll:

GET /api/ingest/atlas-status

As soon as:

• ≥1 legitimate entity exists

OR

• the job becomes idle

stop discovery.

Then:

DELETE /api/ingest/atlas-lock

Do NOT leave discovery running indefinitely.

Do NOT start a second job while the API reports 409 / locked.

If discovery blocks the API event loop:
restart the canonical API Server workflow ONCE.

Then resume from:

GET /api/entities?limit=5

Do not launch repeated discovery attempts.

═══════════════════════════════════════
STEP 12 — LIVE TARGET SANITY CHECK
═══════════════════════════════════════
Select a real entityId from the ledger.

Before Dig:
verify it is a legitimate target.

Reject/degrade:

• unrealistic Forbes-style targets
• junk seed artifacts
• malformed people
• fabricated contacts
• placeholder entities
• entities without credible identity grounding

Do NOT invent a replacement target.

Do NOT manually construct a target merely to make the scoreboard pass.

If no legitimate entity exists:
report that fresh Dig proof cannot be performed and END.

═══════════════════════════════════════
STEP 13 — REQUIRED SINGLE-TARGET DIG PROOF
═══════════════════════════════════════
Run ONE real single-target Dig.

POST /api/ingest/atlas-run

{
“singleTargetId”: <REAL_ENTITY_ID>,
“runResearch”: true,
“researchDepth”: “standard”,
“targetTimeoutMs”: 420000
}

IMPORTANT:
This is the required scoreboard proof.

Do NOT use:

• force_* hops
• scripted hop lists
• prefer-lists
• GROK-PARITY
• hard-coded search sequences
• deterministic target-specific playbooks

The Dig Investigator must choose its own trajectory from the authorized tool surface.

Poll:

GET /api/ingest/atlas-status

until the job becomes idle.

Record:

jobId

Do NOT start another Dig while the current job is active.

═══════════════════════════════════════
STEP 14 — DIG TRAJECTORY / PROVENANCE AUDIT
═══════════════════════════════════════
Inspect the completed target’s trajectory and evidence.

SUCCESSFUL TRAJECTORY SHOULD SHOW MODEL-CHOSEN ACTIONS SUCH AS:

web_search
visit
OSINT tools
pivot
done

The exact trajectory may vary.

FAILURE CONDITIONS include evidence of:

force_*
forced search hops
scripted Dig playbooks
GROK-PARITY
prefer-list routing
fabricated URLs
fabricated contacts
synthetic evidence
unlicensed provider routing
DeepSeek acting as Dig investigator

Deterministic runtime enforcement is allowed.

Deterministic research-path selection is NOT.

If the runtime demonstrates scripted research:
classify as a blocker and do NOT claim scoreboard success.

═══════════════════════════════════════
STEP 15 — CONTACT / EVIDENCE VERIFICATION
═══════════════════════════════════════
Verify contacts/evidence produced by the Dig.

Contacts must have real provenance.

Verify:

• source URLs are real HTTP(S) URLs
• evidence maps to actual sources
• no fabricated URL is accepted
• organization inboxes remain organization scope
• personal contact routes are not fabricated
• promotion is based on evidence/provenance
• identity evidence is sufficient for the entity
• contact-bearing claims are grounded

If the card is empty but Dig wrote valid evidence:
run:

POST /api/entities/rehydrate-contacts

{
“entityId”: <REAL_ENTITY_ID>
}

Then re-check the card/evidence.

Do NOT fabricate a contact merely because the card is empty.

═══════════════════════════════════════
STEP 16 — SCOREBOARD
═══════════════════════════════════════
Run:

bash scripts/replit-scoreboard-check.sh http://127.0.0.1:8080

Expected proof output includes:

mean
n
milestonePass

Scoreboard PASS is valid only when:

• bureauIntegrity is non-critical
• a real entity was used
• single-target Dig completed
• Dig trajectory is genuinely model-chosen
• evidence/provenance is real
• contact promotion is legitimate
• no scripted/forced Dig path was used

If the scoreboard cannot prove those conditions:
do NOT manufacture or reinterpret the result as a pass.

═══════════════════════════════════════
STEP 17 — RIGHT-HAND LIVE SMOKE TEST
═══════════════════════════════════════
Perform a bounded direct smoke test of the configured Right Hand provider if the repository exposes an existing supported health/test path.

The test must be advisory only.

DeepSeek must NOT:

• browse
• execute OSINT
• choose Dig tools
• choose Dig hops
• mutate the research ledger
• become the Dig investigator

The smoke test must confirm the active provider resolves to:

deepseek-ai/deepseek-v4-flash-0731

through:

https://integrate.api.nvidia.com/v1/chat/completions

using:

DEEPSEEK_API_KEY

and the requested reasoning contract:

temperature=1
top_p=0.95
max_tokens=16384
thinking=true
reasoning_effort=high
stream=false

If the live credential/provider call fails:
report the exact non-secret provider error.

Do NOT fall back to the obsolete NVIDIA NIM / z.ai provider.

Do NOT claim fresh provider proof if only static code checks passed.

═══════════════════════════════════════
STEP 18 — FINAL PASS CRITERIA
═══════════════════════════════════════
ALL of the following must be satisfied for a clean PASS:

• latest main tip
• tip is above minimum floor
• install OK
• database push OK
• desk build OK
• index.html exists
• API build OK
• typecheck OK
• no-force-dig check OK
• free-react check OK
• provider-role isolation OK
• Right Hand migration verification OK
• active Right Hand = DeepSeek-V4-Flash-0731
• NVIDIA Integrate transport configured
• DeepSeek reasoning contract configured
• Dig = Groq → Mistral
• DeepSeek NOT in Dig lane
• desk non-blank at /
• healthz non-critical
• Redis connected
• ≥1 legitimate entity
• single-target Dig completed
• Dig has jobId
• trajectory is model-chosen
• real source URLs/evidence exist where claims are promoted
• scoreboard prints mean / n / milestonePass
• no forced/scripted research path detected

If any item fails:
do NOT label the overall run PASS.

═══════════════════════════════════════
FAILURE CLASSIFICATION
═══════════════════════════════════════
Classify blockers precisely.

A — REPOSITORY / TIP
Wrong branch, stale SHA, missing migration script, broken checkout.

B — INSTALL
pnpm/network/registry/lockfile/OOM failure.

C — BUILD / TYPE
Desk build, API build, or TypeScript/module failure.

D — DATABASE
Postgres push/schema failure.

E — PLATFORM
Replit workflow / port / boot failure.

F — REDIS
Redis unavailable, invalid URL, connection failure, quota exhaustion.

G — PROVIDER
Missing Gemini / DeepSeek / Groq / Mistral / search credentials, DeepSeek API failure, obsolete provider still active.

H — INTEGRITY
bureauIntegrity=critical or provenance/integrity violation.

I — FREE-REACT
Free-ReAct or no-force-dig invariant failure.

J — RESEARCH
No legitimate target, Dig timeout, no valid evidence, or failed model-selected trajectory.

K — SCOREBOARD
Scoreboard cannot establish mean / n / milestonePass from legitimate single-target proof.

Never hide one class behind another.

═══════════════════════════════════════
ANTI-REGRESSION RULES
═══════════════════════════════════════
Do NOT:

• revert DeepSeek to NVIDIA NIM/Nemotron
• revert DeepSeek to z.ai/GLM
• route DeepSeek into Dig
• route Gemini into Dig
• replace Groq → Mistral
• add forced Dig hops
• add scripted research playbooks
• add prefer-lists
• add GROK-PARITY
• fabricate a target
• fabricate evidence
• fabricate contacts
• fabricate URLs
• ask for DATABASE_URL
• ask for REDIS_URL_2–5
• print secrets
• create another Replit App
• create another frontend workflow
• redesign Apex Atlas
• silently downgrade a failed live provider test
• claim runtime proof from static source inspection alone

═══════════════════════════════════════
FINAL REPORT — ONE MESSAGE ONLY
═══════════════════════════════════════
Do NOT send progress commentary.

Do NOT send multiple reports.

After all executable work is complete, output ONE final report and END.

Use exactly this structure:

tip SHA:
public URL:
secrets configured: yes/no (names only; NO VALUES)

provider architecture:
Boss:
Right Hand:
Dig:

right-hand migration:
DeepSeek model:
NVIDIA Integrate:
reasoning contract:
live provider smoke:

install OK:
database push OK:
desk index.html:
api build OK:
typecheck OK:
no-force-dig:
free-react:
provider-role check:

healthz status:
healthz integrity:
redis:

desk non-blank:
entityId:
jobId:

Dig trajectory:
provenance/evidence:
contacts:

scoreboard mean / n / milestonePass:

overall result:
PASS / BLOCKED

blockers:
exact non-secret error text if any

If BLOCKED:
state the precise blocker and STOP.

If PASS:
state PASS and STOP.

END.

This version is the one I’d use for the Replit run: migration happens before build, the obsolete NVIDIA/z.ai Right-Hand path is fail-closed, and the live Dig proof remains Groq → Mistral rather than accidentally turning DeepSeek into the investigator.
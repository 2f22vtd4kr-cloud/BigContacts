# Volume 283 — Mandate Checklist for Future PRs

Before merging research or desk changes, answer:

1. Does this **script dig tool choice**? If yes → reject unless det recovery when **all** dig LLMs fail.
2. Does this **write phone/email**? Who owns the column vs dig?
3. Does this **drop org/IR/notice** routes from the UI? If yes → reject (maximum public surface).
4. Does this **claim quality** without integrity/scoreboard? If yes → reject.
5. Does this add an **agent**? New tools/policy or only a renamed phase?
6. Does this help **live scoreboard / COMPARE** or only vanity UI?

If unclear, cite vols 274–280 in the PR description.

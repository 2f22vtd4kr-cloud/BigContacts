# Volume 323 — JobId Binding

Live steps and DigSpans must carry jobId so Reactor and atlas-status show the same run. Secondary dig and discovery agent paths included. Stop clears spans for that jobId.

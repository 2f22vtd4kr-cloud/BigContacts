# Volume 601 — Replit Boot Minimal

## Steps
1. git pull origin main
2. Confirm tip SHA
3. Secrets already in Replit (do not re-ask full list if present)
4. pnpm install if needed
5. pnpm --dir artifacts/apex-finder run build
6. Restart API on 8080 serving desk + /api
7. GET /api/healthz — integrity not critical
8. Optional smoke: Dig contacts one id → Stop

## Do not
Re-import repo from zero · enable auto-pipeline · claim success on blank desk · start long research without integrity ok

# Volume 58 — Security and Secrets Handling

**Part of:** APEX_ATLAS_MASTER_BUREAU_PLAN

- Secrets only in Replit Secrets / env — never commits, never chat logs
- Rotate GitHub PAT if exposed in remote URLs or transcripts
- Do not echo API keys in trajectory summaries
- Public desk must not expose secret values in HTML
- Fail closed on missing keys with observation text, not exception stacks containing env

# Volume 321 — Source Priority Stack

Protected dig/notice sources outrank issuer CIK and generic registry switchboards when writing `entities.phone`. Implementation: `phone-source-priority` / `shouldBlockIssuerOverwrite`. Planning: never invert this stack to “prefer any EDGAR phone.”

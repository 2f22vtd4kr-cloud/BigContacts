# Volume 156 — Related Person Surface

## Intent

Same-issuer peers, co-filers, officers are **outreach alternatives**, not automatic identity merges.

## Rules

- Evidence rows with role `same_issuer_peer` / related
- UI related strip: name only + optional “open entity”
- Never copy peer phone onto primary target without separate dig
- Collision gate on names

## Dig behavior

Model may search related names **if it chooses**; secondary expand may seed review-only anchors. No force_related_person controller.


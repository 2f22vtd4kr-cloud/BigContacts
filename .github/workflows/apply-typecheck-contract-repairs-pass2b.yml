name: Apply Typecheck Contract Repairs Pass 2B
on:
  push:
    branches: [main]
    paths:
      - "scripts/apply-typecheck-contract-repairs-pass2b.mjs"
      - ".github/workflows/apply-typecheck-contract-repairs-pass2b.yml"
permissions:
  contents: write
jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      - run: node scripts/apply-typecheck-contract-repairs-pass2b.mjs
      - run: |
          if git diff --quiet; then echo "No source changes"; exit 0; fi
          git config user.name "apex-typecheck-bot"
          git config user.email "apex-typecheck-bot@users.noreply.github.com"
          git add artifacts/api-server/src/src/lib/discovery-agent.ts artifacts/api-server/src/src/lib/case-bureau.ts artifacts/api-server/src/src/routes/research/cases.ts
          git commit -m "fix: complete second-pass discovery case type repairs"
          git push origin HEAD:main

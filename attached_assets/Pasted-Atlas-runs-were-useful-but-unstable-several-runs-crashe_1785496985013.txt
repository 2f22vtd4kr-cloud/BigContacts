Atlas runs were useful but unstable: several runs crashed, some provider quotas were exhausted, and data-quality filters were patched repeatedly. I’m not going to trust the old “ready” claim blindly. I’m validating the current code path, active-job guard, auto-pipeline setting, toolchain, and secret presence first; then I’ll recover only the required API/web services and launch Atlas once.
Preflight found two blockers before Atlas can safely launch: the imported workspace has no node_modules, and the API/web workflows are failed. The 30 secret slots are present, auto-ingestion is still disabled, and the Atlas route has an active-job guard. I’ll install the locked dependencies, verify the Python research tools and builds, restart only Redis/API/web, then check health and the database before launching the controlled discovery-first Atlas run.
The root cause is confirmed: this is a fresh import with dependencies absent, not an Atlas code failure. I’m restoring the frozen lockfile install now; this does not call research providers or consume their quotas. After that I’ll run the API/web builds and tool checks before starting services.
Dependencies are restored successfully. I’m now running the non-research verification layer in parallel: API build, web build, TypeScript checks, database schema synchronization, and the mandatory Holehe/Maigret availability check. None of these invokes Atlas or provider APIs.
The builds are clean, and the Python tools are verified. The only check failure is a workspace typecheck artifact issue caused by the fresh import (lib/db and lib/api-zod declaration outputs are not built), not an Atlas source failure. I’m fixing that verification prerequisite, checking the development database without modifying data, and then I’ll bring up only the API and web services.
Database connectivity and Redis are healthy. The fresh import’s shared-library typecheck is not a runtime blocker because those packages intentionally expose source files and have no build scripts; I’m confirming the schema/data counts, then I’ll restart the API and web workflows in parallel. Auto-pipeline remains false, so merely bringing the API up will not trigger research.
The database is reachable but its schema is not applied in this fresh import (entities does not exist), so launching Atlas now would fail immediately. I’m applying the existing Drizzle schema—no research data is being created—then I’ll restart API/web and verify health before the Atlas launch.
The schema is now applied successfully. I’m starting only the required Redis/API/web services in parallel (not mobile/mockup), then I’ll verify /api/healthz, the Atlas status endpoint, and the empty/clean database before firing the background run.
Redis, API, and web are now running. I’m doing the final live preflight now: health, Atlas lock/status, database counts, and startup logs. If the lock is clear and the API reports healthy, I’ll launch one controlled discovery-first Atlas job in the background with bounded concurrency and research limits to avoid repeating the prior OOM/quota problems.
Final preflight is green: API health is 200, Redis is healthy with slots 2–5 active (slot 1 correctly quarantined for quota exhaustion), the database is empty, the Atlas lock is idle, the semantic model is warm, and Holehe/Maigret are verified. I’m applying a conservative launch profile now—discovery-first, FAA skipped, smaller enrichment batches, three diversified categories, and MCTS capped at five—to avoid the prior memory crash while still running the full background pipeline.
I found one real tuning defect before launch: the route accepts broadCategories, but the orchestrator currently ignores it and would run all 21 discovery sources despite the “3 categories” setting. That would recreate the previous overrun risk. I’m correcting that small control-path bug, rebuilding/restarting the API, and then launching with the bounded profile so the requested settings actually take effect.
The bounded-source fix is applied. I’m rebuilding the API and rerunning the API typecheck now; if both pass, I’ll restart the API once, confirm the new route is serving, and launch a single background Atlas job with the bounded settings.
The API build is clean; the remaining typecheck failure is the same fresh-import project-reference issue (237 cascaded errors because lib/db and lib/api-zodhave no emitted declaration outputs), not a failure in the Atlas change. I’m restarting the API to load the bounded-source fix, then I’ll launch exactly one background run and immediately verify its job state.
The API restarted cleanly with the bounded discovery fix loaded. I’m doing one last live health/lock check, then I’ll submit a single background Atlas run with: discovery-first enabled, FAA and land-registry downloads disabled, three broad categories plus registry anchors, batch size 50, Phase J batches of 25, and MCTS limited to five leads.
All required services are healthy and the Atlas lock is clear. I’m launching one background run now with the bounded profile we just verified; after the POST returns, I’ll confirm the job is actually running and that it entered the expected first phase.
Atlas has been accepted in the background as job af2ce636-bb70-4e6a-887f-123bb65ceedb. I’m confirming it is actively running, entered the bounded discovery loop, and did not trigger a duplicate job or startup scheduler.
The background run is confirmed healthy: job af2ce636-bb70-4e6a-887f-123bb65ceedb is running in the expected bounded loop, with 9 total source rounds (3 broad categories plus registry anchors), and no duplicate job was created. I’m giving it a short stabilization window to confirm the first discovery round produces valid records, while checking the live dashboard and logs.
Atlas is live and the UI is rendering the first 14 discovered records. The first quality gate is working: six noisy candidates were rejected before insertion. I also found an important runtime limitation in the logs: Perplexity keys are returning quota errors and Gemini is rate-limited, while Exa is returning data; I’m checking whether Groq/Tavily rotation and fallback are carrying the run before I call this stable.
The launch is stable and producing records, but the live logs exposed a concrete tuning issue worth addressing after this run: Perplexity is quota-exhausted, Gemini is rate-limited, and the pipeline is still attempting those calls before falling back to working providers. I’m recording the live state now and checking whether the current run is progressing cleanly before deciding whether this needs an immediate stop or a safe follow-up hardening pass.
The run is stable, but the first enrichment batch surfaced a quality concern that I’m checking rather than ignoring: one record has a chamber membership address and another has a likely business switchboard number. I’m tracing the contact-validation and promotion path now while Atlas continues in the background, then I’ll either let it proceed or stop it if those contacts are being incorrectly promoted as personal evidence.
The run is still progressing normally through the first source cohort, with Exa, Tavily, and Groq producing usable enrichment while Perplexity/Gemini failures are being handled non-fatally. I’m validating the two suspicious contacts against the stored evidence and promotion rules now; I won’t interrupt the active Atlas job unless the data is being promoted incorrectly.
The run is still healthy, and the evidence path confirms a real hardening gap: deep-web AI results are written directly into entities.email/phone without the same public-contact validation used elsewhere. I’m checking the exact evidence schema and current rows now; if confirmed, I’ll let this bounded run finish rather than interrupt it, then patch the promotion guard before any subsequent run.
The run is continuing cleanly through enrichment, and I’m not changing code underneath it. The suspicious contact audit query only failed because this schema version has no attribution_score column; it did not alter data. I’m rerunning the audit against the actual columns and will let this bounded first cohort complete before applying any promotion hardening.
The live evidence audit confirms the concern: some organization-scoped contacts are being promoted into the entity-level contact fields and marked as direct candidates. I’m letting this bounded 14-record cohort finish its current pass so it exits cleanly, then I’ll apply the validation guard and reclassify only the affected records before any broader continuation.
The audit found a real correctness issue, so I’m stopping this bounded run before it processes more records. The API and web services will remain up, but Atlas will be paused with the partial cohort preserved; I’ll then patch the contact-promotion guard and re-run only after organization contacts can no longer be marked as direct personal leads.
Atlas is now paused cleanly with the partial cohort preserved. I’m narrowing the fix to the promotion boundary: normalize email/phone values, reject organization-scoped evidence from entity-level personal fields, and keep those rows auditable as evidence instead of treating them as direct leads. Then I’ll recheck the affected cohort, rebuild, and restart only the API workflow.
I’m applying the narrow fix now: organization-only evidence will remain in the audit table, but it will no longer become the entity’s personal email/phone. I’m also adding the missed shared-inbox prefixes (including membership@) and then I’ll clean only the paused cohort’s clearly organization-scoped fields before rebuilding.
The first patch partially applied only the shared inbox update; the larger enrichment patch was rejected cleanly, so no half-applied web-enricher change exists. I’m adjusting the fix to the safer common boundary in the Atlas orchestrator, where AI output is promoted into durable entity fields, and I’ll verify the exact evidence metadata before editing.
The promotion guard is in place. I’m compiling it now, then I’ll restart the API and run a targeted data cleanup for the paused cohort: organization-only email/phone fields will be cleared, while all provenance rows remain intact for review.
The API build passes with the guard applied. I’m now restarting the API so the fix is live, cleaning only the two confirmed organization-only promotions in the paused cohort, and running the existing web-enricher regression tests plus a post-restart health check.
The new API build is live and the regression suite passes (51 tests). I found the paused records still contain the old promoted values, as expected; I’m removing only those clearly organization-scoped fields and any matching contact-cache entries, then I’ll verify health, data, and the stopped Atlas lock.
The fix is live and health checks are clean. I’m finishing the data repair now: clearing only the two organization-only promotions from entities 13 and 14, preserving their evidence rows, and removing any matching permanent cache records so a restart cannot restore the stale values.
You've reached your daily free quota limit. It will reset at 12:00 AM UTC.

CONTINUE FROM YOUR LAST REPORT.

Do NOT start over.

Do NOT reinstall the project.

Do NOT reread the entire repository.

Do NOT redo the successful test/build/audit work unless a later change requires it.

The previous run already established:

* 419/419 Vitest passing
* Typecheck PASS
* API build PASS
* build idempotence PASS
* database/schema PASS
* Redis audit PASS
* architecture guards PASS
* API boot PASS
* PostgreSQL healthy
* Redis healthy
* Bureau integrity baseline PASS

Those are NOT the current problem.

The current objective is:

CAPTURE THE ACTUAL LOCAL FIXES, THEN PROVE THAT APEX ATLAS CAN CONDUCT A REAL BUREAU RESEARCH TRAJECTORY.

==================================================

1. FREEZE THE CURRENT WORKSPACE
    ==================================================

Before doing anything else:

git status –short
git diff –stat
git diff
git rev-parse HEAD
git rev-parse origin/main
git log -10 –oneline

DO NOT:

* reset
* checkout away changes
* clean
* stash
* commit
* push

I handle GitHub commits.

The local changes from your previous run MUST remain intact.

==================================================
2. INVENTORY EVERY LOCAL CHANGE

Your previous report says you made multiple fixes locally.

I need the EXACT local diff now.

For every changed file, report:

FILE:
WHAT CHANGED:
WHY:
WHICH FAILURE/CONTRACT REQUIRED IT:
TEST THAT PROVES IT:

Do not summarize away code changes.

Use the actual git diff.

Do not claim a fix is committed unless it is actually present in origin/main.

==================================================
3. DO NOT MAKE NEW CHANGES YET

First determine whether the existing local diff is already sufficient for the current baseline.

Run only the minimum verification necessary to establish that the existing local state still has:

419/419 tests
typecheck PASS
build PASS
architecture PASS
Redis PASS

Do not spend time repeating every successful audit individually unless something changed that affects it.

If all remain green, proceed immediately to the live Bureau objective.

==================================================
4. THE REAL PROBLEM

The earlier live Bureau run failed at the actual research layer:

discovery = 0
searches = 0
visits = 0

The Investigator did not establish a real research trajectory.

The previous run also observed:

Groq = reachable
Mistral = HTTP 429

Do NOT assume that the 429 is automatically the root cause.

Trace the actual execution path.

We need to know:

1. Did Boss create the correct mission?
2. Did discovery actually invoke the Investigator?
3. Which Investigator model/provider was selected?
4. Was the model invocation attempted?
5. Did it succeed?
6. What action did the Investigator select?
7. Was that action dispatched?
8. Which research tool was invoked?
9. Did the tool perform a real HTTP/search observation?
10. Was the observation persisted?
11. Was the result returned to the Investigator?
12. Did the Investigator continue or terminate?
13. Was a real candidate established?
14. Did evidence/provenance survive?
15. Did Bureau receive the result?

Trace the REAL code path and logs.

==================================================
5. RUN THE CANONICAL BUREAU AUDIT

Read:

docs/RUN_BUREAU.md

Follow its procedure exactly.

Do not invent a substitute smoke test.

Do not manufacture a target.

Do not fabricate a candidate.

Do not fabricate evidence.

Run ONE clean Bureau audit.

Capture:

* run/job ID
* mission
* Boss invocation
* discovery result
* Investigator invocation
* selected model/provider
* selected action
* tool invocation
* HTTP/search observations
* candidate(s)
* evidence
* provenance
* contact route
* final Bureau state

==================================================
6. LIVE RESEARCH SUCCESS CRITERIA

A Bureau run is successful ONLY if the following actually occurs:

Human mission
↓
Boss
↓
Discovery brief
↓
Investigator
↓
model-selected research action
↓
real research tool
↓
real external observation
↓
real source
↓
real person/candidate
↓
identity evidence
↓
further Investigator reasoning
↓
source-backed evidence
↓
honest contact route
↓
Bureau result

A job reaching “completed” is NOT sufficient.

An API health response is NOT sufficient.

A populated database row is NOT sufficient.

A model completion without a tool action is NOT sufficient.

==================================================
7. INVESTIGATOR OWNERSHIP

Verify from the actual runtime evidence that the Investigator owns the research trajectory.

The application must NOT secretly execute:

Serper
→ Tavily
→ Exa

or:

Groq
→ Mistral

as a deterministic research sequence.

Do not restore any old forced-provider or forced-hop architecture.

The Investigator selects the action.

Deterministic code may enforce:

* authorization
* budgets
* safety
* persistence
* lifecycle
* provenance
* cancellation
* tool validity

but it must not secretly decide the research strategy.

==================================================
8. PROVIDER FAILURE HANDLING

If Mistral returns HTTP 429 again:

DO NOT endlessly retry it.

Determine whether the production system has another authorized Investigator capability available.

If Groq is healthy and the architecture permits it to serve as Investigator, determine whether the system correctly uses it.

If the system cannot proceed because the selected Investigator provider is unavailable, report that honestly.

Do NOT:

* fake a successful model result
* silently substitute another model while reporting the original model
* fabricate a research trajectory
* bypass provider attribution

If this is a real application bug, fix it.

If it is genuine external provider capacity, document it as an external blocker.

==================================================
9. IF DISCOVERY STILL PRODUCES ZERO ACTIONS

This is the most important diagnostic branch.

If you again see:

searches = 0
visits = 0

DO NOT simply retry the Bureau job.

Trace why the Investigator did not produce an action.

Inspect:

* Investigator prompt/input
* available tool definitions
* tool schema
* provider request
* provider response
* response parsing
* action extraction
* action validation
* action dispatch
* discovery state transition
* termination condition

Determine the FIRST point where the expected research trajectory disappears.

Fix that root cause.

Then run ONE clean Bureau retry.

==================================================
10. REAL WEB OBSERVATION

For a successful research action, prove that the system actually observed an external source.

Record:

* URL/domain
* HTTP/tool action
* timestamp if available
* observation persistence
* provenance linkage

Do NOT use synthetic observations.

Do NOT treat a model-generated URL as evidence that the URL was actually visited.

Do NOT treat model knowledge as source observation.

==================================================
11. REAL PERSON / IDENTITY

If discovery produces a person:

verify:

person identity
+
source
+
observed evidence
+
identity attribution

A title, role, or company name alone is not sufficient.

Do not admit:

“CEO”
“Founder”
“Partner”

as a person identity unless the evidence actually identifies a person.

Do not use proxy_table or deterministic page-text extraction as a substitute for research.

==================================================
12. CONTACT PROVENANCE

If a contact route is produced, verify its scope.

Personal route:

must be attributable to the person.

Organization route:

must remain an organization route.

Do not convert:

info@
contact@
press@
generic company form

into a personal contact.

No inferred personal emails.

==================================================
13. ONLY FIX REAL DEFECTS

If the live Bureau run exposes a genuine production defect:

* read the relevant production file completely
* read the relevant test completely
* identify root cause
* make the smallest correct fix
* preserve architecture
* run the focused test
* run full Vitest
* run affected build/guard
* rerun ONE clean Bureau audit

Do NOT fix cosmetic warnings first.

Do NOT refactor unrelated code.

Do NOT chase the duplicate frontend polling warning unless it blocks Bureau.

==================================================
14. NO COMMIT / NO PUSH

Again:

DO NOT COMMIT.

DO NOT PUSH.

I will commit the verified changes myself.

Leave all valid changes in the working tree.

==================================================
15. FINAL REPORT

At the end report:

CURRENT GITHUB BASE

* HEAD:
* origin/main:

LOCAL CHANGES

* exact files:
* exact purpose:
* exact tests:

BASELINE

* Vitest:
* typecheck:
* build:
* idempotence:
* Redis:
* architecture:

BUREAU RUN

* run ID:
* mission:
* Boss:
* Investigator:
* provider:
* model:
* selected action:
* searches:
* visits:
* HTTP observations:
* candidates:
* evidence:
* provenance:
* contacts:
* final state:
* integrity:

IF FAILED:

* FIRST FAILURE POINT:
* ROOT CAUSE:
* APPLICATION BUG or EXTERNAL PROVIDER ISSUE:
* FIX MADE:
* VERIFICATION:

REMAINING BLOCKERS

* exact list

UNCOMMITTED DIFF

* exact summary

==================================================
DEFINITION OF DONE

Do not tell me Apex Atlas is working merely because:

419 tests pass
or
the API is healthy
or
Redis is healthy
or
Bureau says “completed”.

The actual finish line is:

A real mission enters Apex Atlas,
Boss establishes the mission,
the Investigator actually reasons about the research,
the Investigator selects a real research action,
the tool actually executes,
a real external source is observed,
a real candidate is established,
evidence and provenance are preserved,
and Bureau returns an honest result.

If that works:

STOP.

Do not perform unnecessary cleanup.

Do not commit.

Report the evidence.

START FROM THE CURRENT WORKSPACE NOW.
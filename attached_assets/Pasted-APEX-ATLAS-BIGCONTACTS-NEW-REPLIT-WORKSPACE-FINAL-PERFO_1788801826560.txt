APEX ATLAS / BIGCONTACTS

NEW REPLIT WORKSPACE

FINAL PERFORMANCE + LIVE BUREAU VALIDATION

Repository:

https://github.com/2f22vtd4kr-cloud/BigContacts

Current GitHub main has already been audited and repaired.

Expected current HEAD:

c68aba0f18b4de6db40c202f0b4e6805e48f3a58

DO NOT blindly assume this remains HEAD. First fetch origin and report the actual current HEAD.

============================================================
MISSION

This is a fresh Replit workspace.

Apex Atlas is NOT being rebuilt.

The architecture already exists.

Your mission is now:

1. Import the current GitHub repository.
2. Prove the repository is intact BEFORE modifying anything.
3. Validate the canonical build and tests.
4. Validate the Bureau coordination architecture.
5. Obtain fresh provider keys from me where required.
6. Run the real coordinated Bureau.
7. Measure quota economics and research quality.
8. Hunt for runtime/coordination/performance bugs.
9. Fix genuine bugs.
10. COMMIT AND PUSH every production fix yourself.
11. Verify the pushed commit exists on GitHub.
12. Only then provide the final evidence-based verdict.

Do NOT create another artificial architecture cycle.

Do NOT redesign Apex unless a real defect requires it.

Do NOT spend provider quota before the repository/build is proven healthy.

============================================================
PHASE 0 — GITHUB INTEGRITY CHECK

Immediately run:

git fetch origin
git status –short
git rev-parse HEAD
git rev-parse origin/main
git log -20 –oneline

Synchronize to origin/main.

Then inspect GitHub history around:

c68aba0f18b4de6db40c202f0b4e6805e48f3a58

CRITICAL:

Before running any canonical transformation, verify that:

artifacts/api-server/src/lib/agentic-web-research.ts

is the FULL Investigator implementation.

It MUST NOT be truncated.

Compare its size/structure against the preceding intact version if necessary.

The Investigator must retain its complete:

* ReAct loop
* research tools
* search capabilities
* website visitation
* identity investigation
* contact investigation
* evidence handling
* trajectory handling
* stopping logic
* provider/model selection.

If this file is unexpectedly truncated, STOP.

Do not build.

Do not overwrite it with a generated artifact.

Report the corruption and restore from the last known-good GitHub source.

============================================================
PHASE 1 — PROTECT AGAINST CANONICAL-TRANSFORM CORRUPTION

Before running:

pnpm run build

inspect the canonical transformation scripts.

The build transformations MUST be idempotent.

Run the relevant transformation scripts once.

Then inspect the diff.

Expected behavior:

first application:
intentional canonical changes only.

second application:
NO additional source changes.

If running the same build transformation twice produces another diff, STOP and diagnose it.

This is mandatory.

In particular inspect:

scripts/apply-agentic-llm-telemetry.mjs

and verify that it cannot truncate or replace an entire source file.

It must perform bounded, targeted transformations.

============================================================
PHASE 2 — READ THE CANONICAL DESIGN

Read:

docs/context.md
docs/RUN_BUREAU.md
docs/APEX_ATLAS_PERFORMANCE_PLAN.md

Then inspect the actual implementations of:

Boss
Right-hand / DeepSeek
Investigator
LLM provider pool
LLM telemetry
timeout handling
provider retry handling
provider gate
shared Bureau state
Boss context compaction
Right-hand context compaction
action queue
evidence/provenance
contact admission
research lifecycle.

The architecture must remain:

RIGHT-HAND
→ diagnoses the investigation

BOSS
→ commands/integrates the investigation

INVESTIGATOR
→ autonomously executes research

The three are NOT three independent researchers.

============================================================
PHASE 3 — SHARED MIND

Verify that Boss and Right-hand receive meaningful mounting research data as the investigation progresses.

The shared state must allow them to reason over:

* discoveries
* evidence
* unresolved questions
* contradictions
* failed research paths
* identity confidence
* contact routes
* previous decisions
* Investigator trajectory
* current action frontier
* stagnation
* progress.

The Right-hand must identify:

WHAT ARE WE MISSING?

The Boss must decide:

WHAT SHOULD WE DO NEXT?

The Investigator must decide:

HOW SHOULD I EXECUTE THAT MISSION?

Do not make them independently repeat the same research.

============================================================
PHASE 4 — QUOTA DISCIPLINE

The objective is NOT simply minimizing tokens.

The objective is:

MAXIMUM USEFUL INTELLIGENCE
PER UNIT OF COMPUTATION.

Never reduce model reasoning merely to make quota statistics look good.

Instead detect genuine waste:

* duplicate searches
* duplicate model calls
* duplicate provider attempts
* retry fan-out
* abandoned requests
* timeout leaks
* repeated observations
* unnecessary summaries
* oversized context
* unnecessary Boss reasoning
* unnecessary Right-hand reasoning
* redundant Investigator work.

A legitimate failed provider request is not automatically waste.

An unnecessary request is waste.

============================================================
PHASE 5 — LLM AUTONOMY

Do NOT hard-code:

* search order
* queries
* contact discovery strategy
* fixed pivots
* fixed stopping rules
* research routes.

The models must be allowed to use their trained reasoning capabilities.

The deterministic system provides:

* tools
* memory
* evidence
* provenance
* safety
* lifecycle
* budgets
* coordination
* integrity.

The models provide the intelligence.

============================================================
PHASE 6 — PROVIDER KEYS

Before live research, check the required Replit Secrets.

Expected provider secrets include:

GEMINI_API_KEY
DEEPSEEK_API_KEY
GROQ_API_KEY
MISTRAL_API_KEY

If any are missing or expired/exhausted, STOP before live research and ask me for fresh keys.

NEVER display secret values.

NEVER write secrets to files.

NEVER commit secrets.

Use Replit Secrets.

The previous Bureau experiment exhausted provider quota before completion, so fresh unused quota is expected.

============================================================
PHASE 7 — BASELINE

Run:

* typecheck
* canonical build
* full Vitest
* architecture guards
* coordination guard
* performance guards
* API startup
* /api/healthz
* PostgreSQL health
* Redis health.

Do not claim success based only on healthz.

Record exact results.

If something fails:

classify it as:

A. environment
B. dependency/workspace
C. provider
D. generated/canonical build issue
E. actual source defect.

Fix actual source defects.

============================================================
PHASE 8 — COMMIT DISCIPLINE

THIS IS MANDATORY.

If you make ANY production-code fix:

1. inspect git diff
2. run focused test
3. run relevant regression guards
4. run full tests if appropriate
5. git status
6. git add
7. git commit
8. git push origin main
9. git fetch origin
10. verify origin/main contains the commit
11. report commit SHA.

NEVER leave a claimed fix only in the Replit workspace.

NEVER say “fixed” unless it is committed and pushed.

At the end:

git status –short

must be clean unless there is a deliberate non-source runtime artifact.

============================================================
PHASE 9 — START THE BUREAU

Only after the baseline is clean.

Start the canonical API according to:

docs/RUN_BUREAU.md

Verify the service.

Then run the Bureau against:

ANDY CANTWELL

This is a BLIND TEST.

Do NOT provide Apex with:

* my independent findings
* his contact details
* his website
* search queries
* source URLs
* known answers.

Apex must discover them itself.

============================================================
PHASE 10 — LET THE BUREAU ACTUALLY RESEARCH

Do not terminate merely because it found the target identity.

Allow the Bureau to investigate:

* identity
* career
* projects
* relevant affiliations
* authoritative sources
* independent corroboration
* relationships/context
* public contact routes
* attributable direct contact where publicly available.

Do not force it down a predetermined path.

The Bureau decides when evidence is sufficient.

============================================================
PHASE 11 — FULL LLM TELEMETRY

Capture every physical LLM request.

At minimum:

run ID
role
iteration
provider
model
attempt
retry index
HTTP status
success/failure
timeout
input tokens
cached input tokens where available
output tokens
total tokens
latency.

Do NOT log:

API keys
secrets
raw prompts
raw responses
private credentials.

Telemetry must cover ALL THREE reasoning layers:

Boss
Right-hand
Investigator.

If current instrumentation does not provide equal visibility for all three, identify this as a measurement gap and fix it if it can be done safely without changing research behavior.

============================================================
PHASE 12 — RESEARCH-VALUE TELEMETRY

Associate LLM computation with actual research progress.

Measure:

* searches
* visits
* observations
* new evidence
* new sources
* identity resolution
* contradictions resolved
* contact routes
* verified contacts
* meaningful pivots
* redundant actions.

The critical metric is:

USEFUL INTELLIGENCE / COMPUTATION

not:

NUMBER OF LLM CALLS.

============================================================
PHASE 13 — COORDINATION AUDIT

After the live run inspect the actual trajectory.

Ask:

Did Right-hand understand what Investigator had already done?

Did Right-hand identify genuine gaps rather than repeat research?

Did Boss understand the mounting evidence?

Did Boss choose useful next assignments?

Did Investigator receive the current mission and relevant context?

Did any layer contradict another?

Did any layer research the same matter unnecessarily?

Did information get lost during context compaction?

Did the Bureau move toward higher-value unresolved questions?

Did the chain behave like ONE ORGANISM?

============================================================
PHASE 14 — CONTACT QUALITY

Contacts are a primary quality metric.

Classify:

PERSONAL DIRECT
PROFESSIONAL DIRECT
ORGANIZATIONAL
GENERIC
SOCIAL
CONTACT FORM
INFERRED
UNVERIFIED.

Never call an inferred address verified.

Never promote:

info@
contact@
press@

into a personal contact.

A publicly attributable direct contact is high-value evidence.

============================================================
PHASE 15 — EVIDENCE QUALITY

For every major claim evaluate:

CLAIM
→ SOURCE
→ OBSERVATION
→ IDENTITY LINK
→ PROVENANCE
→ CONFIDENCE.

Prefer primary and authoritative sources.

Require corroboration where appropriate.

Do not manufacture certainty.

============================================================
PHASE 16 — ANTI-STAGNATION

Inspect whether consecutive iterations actually move the research frontier.

Useful movement includes:

new evidence
new source
new identity information
new contact
new relationship
new hypothesis
contradiction resolution.

If the Bureau stagnates:

do NOT hard-code a deterministic pivot.

Instead ensure stagnation is surfaced to Boss/Right-hand/Investigator so the models can reason about:

pivot
corroborate
revisit
contact investigation
stop.

============================================================
PHASE 17 — FAILURE SEMANTICS

Verify:

429
→ provider capacity failure

401/403
→ terminal provider failure

timeout
→ incomplete/degraded

provider outage
→ degraded/failure

invalid model response
→ bounded repair/failure.

Never convert failure into fake success.

Never call an incomplete Bureau run complete.

============================================================
PHASE 18 — SECOND RUN IF QUOTA ALLOWS

If the first run completes successfully and sufficient quota remains:

perform ONE second bounded run.

Do not feed the first run’s answer into the second as ground truth.

The purpose is robustness.

Compare the runs for:

* research trajectory
* quality
* useful findings
* contact discovery
* redundancy
* token economics
* consistency.

If quota does not permit it, do not fabricate a second run.

============================================================
PHASE 19 — BLIND COMPARISON

Only AFTER Apex has completed its research may you compare it against my independent research.

Compare INFORMATION VALUE, not wording.

Evaluate:

identity
career
projects
sources
corroboration
relationships
contacts
contact attribution
provenance
depth
missed opportunities.

The known independent baseline establishes that a publicly attributable direct contact exists.

Do not tell Apex that beforehand.

============================================================
PHASE 20 — FINAL SCORECARD

Report:

TOTAL LLM CALLS
TOTAL INPUT TOKENS
TOTAL CACHED INPUT TOKENS
TOTAL OUTPUT TOKENS
TOTAL TOKENS
RETRIES
429s
TIMEOUTS
EXCEPTIONS
AVERAGE LATENCY

BY ROLE:

Boss
Right-hand
Investigator

BY PROVIDER:

Gemini
DeepSeek
Groq
Mistral

RESEARCH:

searches
visits
observations
meaningful findings
verified sources
identity evidence
contact routes
verified direct contacts
provenance completeness.

EFFICIENCY:

useful findings / 1,000 tokens
verified sources / 1,000 tokens
verified contacts / 1,000 tokens
redundant actions / total actions
failed attempts / total attempts
tokens / meaningful research decision.

============================================================
PHASE 21 — FINAL BUG HUNT

Before declaring success inspect:

* coordination
* context compaction
* shared state
* retry behavior
* timeout cancellation
* provider fallback
* telemetry
* evidence
* provenance
* contact classification
* duplicate work
* premature stopping
* endless research
* terminal states
* build idempotence
* generated-source integrity.

If a genuine defect is found:

FIX IT.

TEST IT.

COMMIT IT.

PUSH IT.

VERIFY IT ON GITHUB.

Then rerun the affected validation.

============================================================
PHASE 22 — FINAL GITHUB PROOF

At the very end show:

git rev-parse HEAD
git rev-parse origin/main
git status –short
git log -10 –oneline

HEAD MUST MATCH origin/main.

Working tree must be clean.

List every commit made during this workspace.

============================================================
FINAL VERDICT

Do not say “looks good.”

Do not say “promising.”

Do not declare success because tests pass.

Answer:

1. Is the repository intact?
2. Is the canonical build idempotent?
3. Are all tests passing?
4. Does the Bureau coordinate as one organism?
5. Do Boss and Right-hand reason over mounting data?
6. Do they avoid duplicating Investigator work?
7. Does Investigator remain autonomous?
8. Is quota waste controlled?
9. Are timeout/retry leaks eliminated?
10. Is telemetry sufficient to explain quota consumption?
11. Did Apex perform genuinely deep research?
12. Did it discover substantially more useful intelligence than a single-agent baseline?
13. Did it discover the attributable direct contact?
14. Is evidence/provenance strong?
15. Did the Bureau demonstrate meaningful advantage over a single investigator?
16. Is Apex ready for real-world use?

Then choose ONE:

APEX IS THE BEAST WE DESIGNED.

OR

APEX IS NOT YET THE BEAST WE DESIGNED.

Do not choose the first unless the evidence genuinely supports it.

============================================================
ABSOLUTE RULE

DO NOT SPEND EXPENSIVE PROVIDER QUOTA UNTIL THE REPOSITORY HAS PASSED THE INTEGRITY + BUILD + TEST GATES.

DO NOT MODIFY THE ARCHITECTURE JUST TO “DO SOMETHING.”

DO NOT REDUCE MODEL INTELLIGENCE JUST TO SAVE TOKENS.

DO NOT LEAVE FIXES UNCOMMITTED.

DO NOT CLAIM A FIX THAT WAS NOT PUSHED.

DO NOT HIDE FAILURES.

DO NOT MANUFACTURE SUCCESS.

The goal is simple:

BUILD APEX INTO THE BEST RESEARCH BUREAU THIS ARCHITECTURE CAN ACTUALLY BE.

MAKE BOSS THINK.

MAKE RIGHT-HAND THINK.

MAKE INVESTIGATOR THINK.

MAKE THEM SHARE ONE LIVING CASE.

MAKE THEM MOVE IN ONE DIRECTION.

MAKE EVERY EXPENSIVE MODEL CALL EARN ITS PLACE.

AND MAKE THE FINAL INTELLIGENCE SUBSTANTIALLY BETTER THAN A SINGLE AGENT COULD REASONABLY PRODUCE.

START NOW.
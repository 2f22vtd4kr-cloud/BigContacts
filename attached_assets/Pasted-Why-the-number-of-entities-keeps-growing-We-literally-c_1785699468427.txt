Why the number of entities keeps growing? We literally confirmed that each single target goes through full research, not in batches.
I’m tracing the live Atlas run and the entity-count query together. The key distinction is whether Atlas is adding new discovery candidates, processing a selected target one at a time, or whether the dashboard is counting hidden/review-only records as visible entities.
The live count is already pointing to two separate issues: the dashboard is likely counting all database rows, including hidden review records, and Atlas still has a discovery stage that can add new entities independently of the one-target-at-a-time enrichment loop. I’m checking the exact SQL and orchestration loop now so I can distinguish the UI count bug from the pipeline behavior and fix the mismatch rather than explain it away.
I found the likely mismatch: “full research one target at a time” governs enrichment of an already-selected target, but Atlas also has a separate discovery stage that can create new entity rows before enrichment. The dashboard’s Entities in view metric also appears to count hidden review rows. I’m confirming both paths in the live code and run logs so I can give you the exact reason and correct the behavior.
You’re right to challenge this. The current code does notenforce “discover one target → fully research it → discover the next target.” It enforces “run a discovery source that may create many rows → then fully research those rows sequentially.” That is why the entity count can grow even though enrichment itself is sequential. I’m stopping the run before it adds more records, then I’ll change the orchestration boundary—not just the UI wording.
The run is stopped cleanly. I’m now changing Atlas so discovery admission is single-target scoped: a discovery pass may find many leads internally, but it will admit only one candidate, fully cook that candidate, and only then proceed to the next discovery pass. I’ll also make the dashboard’s entity count exclude hidden review records so the number shown matches “entities in view.”
The root cause is now confirmed in code, not just telemetry:
1. runBroadDiscovery() inserts every accepted candidate from a search round.
2. Registry rounds call Western HNWI ingestion with batchSize: 100, so they can admit many rows before cooking them.
3. Only after that does Atlas run the sequential full-circle enrichment loop.
4. The dashboard count uses count(*) across all rows, including the hidden review records, while the ledger excludes hidden rows.
I’m fixing both contracts: Atlas will admit one target at a time from every source, fully cook it before the next admission, and “Entities in view” will count only visible rows.
I’ve confirmed the orchestrator bug and stopped the unsafe run. One more implementation check is needed: the registry ingestor’s public API, so I can constrain its admission to one row without breaking its existing job bookkeeping.
The stopped run exposed the exact contract violation. I’m applying the fix at the admission points now: broad discovery will admit at most one candidate per source round, registry ingestion will request one record per round, and each admitted row will be fully cooked before the next source round begins. Hidden review records will also be excluded from the dashboard total.
I’m implementing the correction now. Atlas will no longer admit an entire search result set or a 100-record registry batch: each source round will admit at most one new target, run the complete full-circle enrichment and final review for that target, and only then continue. I’ll also fix the dashboard count/cache so hidden review records do not inflate “Entities in view.”
The admission boundary is patched. I’m checking the exact diff for two things before restarting: that dashboard cache invalidation is attached to the hide endpoint (not another entity mutation), and that the one-record registry round still has a valid fallback when a candidate is deduplicated. Then I’ll run tests/build and verify the live count.
One small issue in the first patch needs correcting before verification: the dashboard cache invalidation landed on the star endpoint instead of the hide endpoint, so an already-cached total could remain stale for up to 60 seconds. I’m moving that invalidation to the correct mutation and adding a regression test for the single-admission contract.
The cache invalidation is corrected, and the one-admission adapter is covered. I’m running the focused tests and API build now, then I’ll restart the API, clear the stale dashboard cache, and verify both the visible count and Atlas’s stopped state before documenting and committing the fix.
The fix passes: focused guardrail coverage is now 21/21, and the API build is clean. I’m restarting the API to activate the new admission contract, clearing the cached dashboard total, and then I’ll verify that the visible count is 0 while the 18 hidden review records remain quarantined and Atlas is idle.
The API has restarted with the corrected single-admission contract. I’m verifying the live result now: visible entity count, hidden review count, dashboard cache behavior, and that Atlas stayed stopped rather than restarting automatically.
There’s one more live inconsistency I caught during verification: the API returned the hidden rows even when I requested hidden=false, which means the query parser treats that parameter as absent rather than explicitly false. The dashboard count fix is correct, but I’m fixing this visibility-query contract too so the UI and API agree about what “in view” means.
The dashboard query itself is now correct (count(*) WHERE isHidden = false). The remaining inconsistency is in the entity-list filter: hidden=false is being parsed as a truthy string by the shared query schema, so the API can return the hidden review rows for a request that sounds like the visible view. I’m fixing the boolean query parsing at the schema boundary, then I’ll rerun the live endpoint checks and commit the full correction.
I found the last bug: Zod’s coerce.boolean()interprets the string "false" as true, so the API’s hidden=false request was incorrectly entering the hidden branch. I’m fixing the route to parse boolean query strings explicitly, then I’ll verify visible/hidden counts, run the checks, and commit the complete Atlas correction.
I’m fixing the final query parsing issue at the API route boundary rather than editing the generated schema: "false" will become a real boolean before validation, so visible and hidden entity views cannot be swapped accidentally. Then I’ll verify the actual endpoint responses and commit the full correction.
The final visibility parser fix is in place. I’m running the focused checks and build, then I’ll restart the API so the live endpoints use it, verify visible versus hidden records, update the runtime documentation, and push the complete fix.
The final patch passes 21/21 focused tests and the API build. I’m activating it now, then I’ll confirm that hidden=false returns the visible view, hidden=truereturns only quarantined records, the dashboard says 0 entities in view, and Atlas remains idle until you explicitly relaunch it.
The API is restarted with the final visibility fix. I’m doing the last live verification now, then I’ll update the project records and push the corrected single-target Atlas behavior.
The live verification now shows the intended behavior: Atlas is idle, the visible ledger excludes the 18 quarantined rows, and hidden=true returns those review records only. I’m doing the final state capture—including the actual dashboard JSON and the uploaded screenshot reference—then I’ll update the project notes and push this fix so the behavior is durable.
The live verification uncovered a second stale assumption: the screenshot’s “20” is from before the imported runtime’s existing visible records were counted. The current database has 170 visible entities plus 18 hidden review records, and Atlas is idle. I’m tracing when those 170 were created and which source admitted them so I can separate historical data from the run that violated the intended sequencing, then I’ll finalize the docs and push the fix.
You've reached your daily free quota limit. It will reset at 12:00 AM UTC.
